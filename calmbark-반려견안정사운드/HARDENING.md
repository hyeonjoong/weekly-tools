# HARDENING — 적대적 검증 기록

이 파일은 calmbark 를 조용히 틀리게 만들 수 있(었)는 지점들의 기록입니다.
형식은 라운드 단위입니다: 라운드 0 은 빌드 세션의 자체 점검이고, 이후 라운드는
독립 리뷰어(적대적 패널)가 추가합니다.

---

## 라운드 0 — 빌드 시점 자체 점검 (2026-08-21)

### A. 빌드 중 실제로 잡은 결함

1. **브라운 캐리어의 엔벨로프 랜덤워크가 논문 준수 테스트 ④⑦ 을 깨뜨림.**
   최초 구현은 교과서식 브라운 잡음(누설 적분기, 코너 ~8 Hz)이었다. 에너지가
   DC 근처에 몰리면 신호의 레벨 자체가 비주기적으로 느리게 방황한다(가우스
   엔벨로프 요동 σ/μ≈0.52 의 저주율 성분). 그 결과 예측가능성 자기상관이
   r≈0.4 로 떨어지고, 5–7 dB 짜리 빠른 가짜 "온셋"이 수십 개 검출됐다.
   이것은 지표 결함이 아니라 **소리 설계 결함**이다 — 레벨이 방황하는 텍스처는
   실제로 덜 예측가능하다. 수정: (a) 스펙트럼 기울기(β≈−2)는 유지하면서
   대역폭을 넓혀(one-pole 코너 300 Hz) 요동을 빠른 주율로 밀어냈고, (b) 느린
   AGC(3 Hz 추적기로 나눗셈)로 3 Hz 미만 레벨 방황을 제거했다. 의도적 진폭변조는
   AGC **이후에** 곱하므로 억제되지 않는다. 핑크 캐리어도 80 Hz 고역통과로 같은
   문제를 예방했다.

2. **러프니스 지표 1판이 물리적으로 불가능한 요구였음.** 처음에는 "30–150 Hz
   변조 대역 파워 비율 ≤ 15%"로 썼는데, 광대역 가우스 잡음은 엔벨로프 요동이
   제거 불가능해(위 σ/μ) 어떤 정상 잡음 텍스처도 이 기준을 못 넘는다는 것이
   측정으로 드러났다. 스펙 문구("유의 **피크** 없음")에 맞게 재정의: (a) 대역 내
   이산 피크가 주변 중앙값보다 +10 dB 이상 솟지 않을 것(트레몰로 검출), (b) 대역
   평균 PSD 가 인접한 더 느린 대역(5–25 Hz)보다 높지 않을 것(대역 융기 검출).
   재정의가 지표를 물렁하게 만들지 않았음은 음성 대조 2종(70 Hz 트레몰로,
   30–150 Hz 대역 제한 잡음 변조)이 여전히 걸리는 것으로 증명했다.

3. **엔벨로프 추종기 웜업이 가짜 거대 온셋을 만듦.** 추종기 상태가 0에서
   출발하므로 버퍼 첫 ~0.3 s 에 50–60 dB짜리 가짜 상승이 잡혔다. 측정을 0.5 s
   이후부터 시작하도록 수정. 실제 재생은 앱이 1.5 s 페이드인을 걸므로 버퍼
   첫머리는 어차피 온셋이 아니다.

4. **드론 부분음 구성이 하마터면 Arnal 대역 한복판에 맥놀이를 넣을 뻔함.**
   f + 2f 구성이면 부분음 간격 = f0 = 84–104 Hz — 회피해야 할 30–150 Hz
   러프니스 대역 정중앙에 엔벨로프 변조가 생긴다. 설계 단계에서 계산으로 잡아
   f + 3f(간격 2·f0 = 168–208 Hz > 150 Hz)로 확정했다. 저주파 화성 드론의
   일반적 함정이라 기록해 둔다.

5. **purity grep 이 자기 주석에 걸림.** 순수 모듈 소스에 금지 식별자
   (`Date.now` 등)가 없어야 한다는 테스트가, "Date.now 를 쓰지 않는다"는 주석의
   문구 자체에 걸렸다. 주석을 다른 표현으로 바꿨다. 이 grep 은 주석과 코드를
   구분하지 못한다 — 우회(문자열 조립으로 `window` 참조 등)도 못 잡는다.
   패널은 이 테스트를 "1차 방어선"으로만 취급할 것.

6. **빌드 도중 외부 프로세스가 작업 폴더를 삭제함.** 13:00 경 상위 git 저장소의
   `.git` 이 갱신되면서 미추적 상태였던 calmbark 폴더가 통째로 사라졌다(아마
   외부의 git clean 류 동작). 컨텍스트에 남아 있던 전체 파일로 재생성했고, 이후
   마일스톤마다 스크래치패드에 백업을 떴다. 이 저장소에서 빌드하는 세션은
   같은 위험을 가정해야 한다.

### B. 설계로 미리 막은 함정

- **재생음 자기 트리거 (기획 실패지점 3).** 3중 완화: ① 재생 중 감지 문턱
  +6 dB, ② 재생 중 소음 바닥 갱신 동결(재생음이 바닥을 끌어올려 이후 감도가
  조용히 무뎌지는 2차 함정까지 차단), ③ 재생 중 감지는 `짖음(재생중)` 유형으로
  분리 기록 — 제거가 아니라 **정직한 표시**다. 페이드아웃 중에도 스피커에서
  소리가 나므로 playbackActive 는 페이드아웃 종료까지 유지된다. 전부
  `tests/detector.test.mjs` 로 고정.
- **CSV 수식 인젝션.** `=`, `+`, `-`, `@` 로 시작하는 문자열 셀은 `'` 접두,
  숫자는 비가드(−3.5 가 망가지면 안 됨), RFC 4180 인용. 테스트 고정.
- **루프 경계 클릭.** 잡음: 등파워 크로스페이드(경계 이행 = 인접 샘플 이행).
  드론: 음형 주파수 궤적의 구간 평균을 정수 사이클로 설계(94 Hz × 15 s = 1410)해
  위상이 루프에서 이어짐. 진폭변조는 루프당 정수 사이클. 테스트 고정.
- **`echo | bash 실행.command` 무한 대기.** stdin 이 TTY 가 아니면 스모크
  테스트(기동→curl 확인→종료)만 하고 즉시 exit 0. 포트는 OS 에서 빈 포트를
  직접 받아 충돌을 원천 회피. 서버는 127.0.0.1 전용 바인딩.
- **에피소드 경계값.** "30초 안 재짖음 = 같은 에피소드"의 경계(정확히 30000 ms)는
  새 에피소드로 정의하고 경계값 테스트로 고정.
- **마이크 프라이버시.** 오디오 경로는 AnalyserNode 특징 추출뿐. MediaRecorder /
  fetch / XHR / WebSocket / sendBeacon 은 소스 grep 테스트로 금지. 산출물은
  타임스탬프·dB 숫자뿐이다.
- **백그라운드 탭 감지 정지.** rAF 는 백그라운드에서 완전히 멈추므로 setInterval
  (40 ms)로 구동 — 백그라운드에서도 초당 1회 수준으로는 돈다(거칠어질 뿐).
  사용법에 "탭 전면 유지" 명시.

### C. 알면서 남긴 것 (패널에 미리 자백)

1. **온셋 지표는 8 dB 미만 돌출을 이벤트로 치지 않는다.** 가우스 텍스처의 자체
   요동이 4σ≈7.5 dB 까지 관측되므로(시드 10종 스윕) 그 아래는 원리적으로 구분
   불가라고 판단했다. 6–8 dB 사이의 빠른 팝을 엔진에 심으면 이 테스트는 못
   잡는다 — 텍스처 요동에 마스킹된다는 논거이나, 반박 가능하다.
2. **② 대역 융기 지표의 여유가 얇다.** 시드 10종 스윕에서 worst 0.947 (기준
   1.0). 고정 시드 테스트는 결정론적으로 통과하지만, 임의 시드에서 1.0 을 스치는
   시드가 존재할 수 있다. 기준을 낮추는 대신(원리: "융기 없음" = 1.0) 여유를
   기록으로 남긴다.
3. **app.js(WebAudio 접착층)는 자동 테스트 밖.** 마이크가 차단된 빌드 환경에서는
   수동 스모크(페이지 로드·미리듣기 3종·마이크 거부 경로)만 했다. 특징 추출
   (`bandDbFromAnalyser`)의 대역 합산이 잘못되면 순수 로직이 전부 옳아도 앱은
   틀린다 — 실기기 검증 항목.
4. **프록시 ≠ 심리음향 단위.** asper/acum/DIN 45692 를 계산하지 않는다. 논문
   Table 2 의 정식 파이프라인(Essentia/mosqito)과의 교차검증은 DEBUSSY 로
   가능하지만 이 저장소 범위 밖.
5. **논문 준수는 44.1 kHz 기준.** 48 kHz 는 렌더 스모크만. AudioContext 가
   44.1/48 이외의 샘플레이트를 주는 기기(일부 블루투스)는 미검증.
6. **mulberry32 가 engine.js 와 study.js 에 중복 정의.** 모듈 자기완결(상호
   import 금지)을 위해 의도적으로 중복했다. 두 구현이 어긋나면 각자의 결정론
   테스트가 잡지만, "같은 시드가 두 모듈에서 같은 수열"이라는 교차 보장은 없다
   (필요하지도 않다 — 용도가 다르다).
7. **AGC(레벨 안정화)는 순수한 논문 번역이 아니라 공학적 추가다.** Tier 1
   Event Structure("안정 평균 레벨")의 실행이라고 판단했지만, 원문에 "AGC 를
   쓰라"는 문장은 없다. 정당화는 README 대응표와 engine.js 주석에 있다.
8. **무작위 배정은 블록화가 없다.** 단순 베르누이 50%라 짧은 세션에서 개입/비개입
   불균형이 날 수 있다(예: 6 에피소드가 5:1). 블록 무작위화는 v1 범위 밖 —
   에피소드 수가 적으면 statwise 결과 해석에 주의.

---

## 라운드 1 — 적대적 패널 (2026-08-21)

독립 리뷰어 4명. 중대 10건(C1–C10)·경미 14건(M1–M14) — 전건 수정.
아래는 발견 → 수정 대응. 수정 후 전체 스위트 149개 전부 통과,
`echo | bash 실행.command` 스모크 통과 재확인.

### 중대

- **C1 무작위 모드 대조군 오염** (재생 180초 ≫ 에피소드 간격 30초 — 개입 재생이
  나오는 중에 시작된 "비개입" 에피소드가 소리에 노출된 채 개입여부=0 으로 기록;
  seed 8, 짖음 t=0/10/100s 로 재현됨). → 에피소드 규칙은 군 대칭으로 유지하고
  (비대칭 병합은 그 자체가 편향) ① CSV 에 `재생겹침초`(가청 재생과의 겹침)와
  `후속180초짖음수`(두 군 동일한 180초 고정 창의 짖음 수 — 1차 지표) 열 추가,
  ② README·사용법·화면 요약에 "오염 행 per-protocol 제외 + 1차 지표는 고정 창"
  명시(정직성 절에도), ③ 원장 수정: 새 에피소드가 기존 재생을 연장하면 `재생연장`
  이벤트(새 에피소드 ID)가 남고 `재생종료` 는 마지막 연장 에피소드에 귀속.
  study.js 순수 로직 + 테스트(패널 재현 시나리오 그대로)로 고정.
- **C2 시드 취약 준수** ("어떤 시드든 통과" 주장이 거짓 — brown_waves ② 융기가
  seed 8/15 에서 1.074/1.027, ④ 여유가 seed 18 에서 54.9 ms). → 엔진 재설계:
  **영위상(순방향+역방향 one-pole) 엔벨로프 정칙화** — 출력 엔벨로프를 20 Hz
  영위상 평활본으로 치환해 20 Hz 초과 확률 변조를 주율별 1/(1+(f/20)²)로 억제.
  1차 시도(위상 있는 추적기 비율)는 위상 지연 때문에 분모 대역(5–25 Hz)을 더
  깎아 융기가 오히려 1.1–1.6 으로 악화됨을 스위프로 확인하고 폐기 — 영위상이
  핵심이다. 드론은 바닥 잡음 −20→−26 dB (교차항이 융기 주범). 결과: 26시드
  스위프에서 융기 최악 1.6 → 0.85, ④ 최소 상승 188 ms. 단, 이 융기 수치는
  **스위트 프록시 지표 기준**이다(라운드 2 검증자의 평탄 엔벨로프 지표로는 드론이
  1.0 부근, 비검증 시드 최악 1.05 — 절대 변조폭 σ/μ 5.7%로 brown(7.3%)보다 낮고
  이산 피크 ≤4.1 dB 라 지각적 위험은 아니나, "시드 무관 보장"은 프록시 지표 안의
  보장으로 읽을 것). **검증 시드 16종**
  (`VETTED_SEEDS`)을 엔진에 내장 — 앱 자동 시드는 여기서만 뽑고, 수동 입력은
  `시드(미검증)` 으로 로그. 테스트: 16시드 × 3프리셋 ②(융기 ≤0.90)④(상승 ≥60 ms)
  전수 스위프 + 지표 8종 전체를 시드 3종(재현 시드 8·15 포함)에서 실행.
  README 의 시드 관련 주장을 "정확히 테스트가 강제하는 것"으로 재서술.
- **C3 클릭 맹점** (매초 풀스케일 1샘플 클릭이 7종 전부 통과 — 엔벨로프 평활이
  희석). → ⑧ 크레스트/클릭 가드 신설: 1 ms 창 피크 vs 국소 100 ms RMS ≤ 14 dB
  (클린 스위프 최악 10.5 + 여유) **및** 인접 샘플 차분 크레스트 ≤ 22 dB(최악
  18.3 + 여유). 음성 대조(클릭 주입)로 판별력 증명. **처방과의 편차 자백**:
  진폭 크레스트만으로는 1샘플 클릭을 못 가른다(엔진 자체 가우스 피크 ~10 dB vs
  클릭 ~10–12 dB — 분리 없음). 실제로 클릭을 잡는 것은 차분 크레스트(클릭 ~38 dB)
  다. 둘 다 상한으로 두되, 대조 테스트는 이 사실을 주석으로 명시한다.
- **C4 음소거 보정 → 영구 유령 짖음** (바닥 −120 dB → 문턱 −108 → 모든 프레임이
  짖음, 바닥 적응은 문턱 아래에서만 돌아 회복 불가). → 보정 바닥 ≤ −100 dB 면
  `calibration_failed` — 세션을 시작하지 않고 한국어로 안내("마이크가 음소거이거나
  입력이 없습니다…"). 경계(정확히 −100)·재시도 테스트 포함. 전 프레임이 무효라
  보정이 영영 안 끝나는 변종은 앱 쪽 8초 감시견이 같은 안내로 중단.
- **C5 세션 중 미리듣기가 완화책 전부 우회** (가드 없음 — 보정 오염·가짜 에피소드
  ·미표시 재생). → 세션 중 미리듣기 버튼 비활성 + `togglePreview` 내부 가드 +
  `playbackAudible()` 에 previewNodes 포함(심층 방어). 소스 계약 테스트로 고정.
- **C6 마이크 트랙 사망 무감지** (장치 분리/권한 회수 후에도 "감시 중" 표시).
  → `track.onended` → `오류(마이크종료)` 로그 + 한국어 안내 + 세션 자동 종료.
  정상 종료(앱이 stop 한 트랙)와는 sessionActive 선행 해제로 구분.
- **C7 절전 유령 짖음** (버스트 중 2시간 갭 → "7200초 짖음 1회"). → 프레임 간격
  > 5초면 진행 버스트를 **미방출 폐기**하고 `framegap` 이벤트(앱은 `갭(N초)` 로
  기록). 보정 중 갭은 보정 재시작. 갭 프레임 자체가 새 버스트를 시작하는 것은
  허용(시작 시각이 갭 이후라 유령 지속시간 없음 — 테스트 주석 참조). 사용법에
  갭 로그 안내 추가.
- **C8 FADE_IN 미고정** (1.5→0.02 뮤턴트가 73개 전부 생존) + 슬라이더 램프 추월
  (P2-11). → 소스 계약 테스트: `FADE_IN_SEC ≥ 1`·`FADE_OUT_SEC ≥ 0.5` 강제.
  구조 수정: rampGain(페이드 전용)×volGain(슬라이더 전용) 직렬 체인 — 슬라이더는
  volGain 만 만지므로 페이드인 중 조작해도 램프를 추월할 수 없다. 볼륨 핸들러가
  rampGain 을 안 만진다는 것도 소스 검사(주석 제외)로 고정.
- **C9 README 인트로의 42–80 BPM 오귀속** (논문 예시값은 ~60–80 BPM; 42–80 은
  구현 허용 대역). → KR/EN 인트로 모두 "문헌 예시 60–80 BPM(구현 허용 42–80)"
  으로 수정. M9(드론 48→60 BPM 상향)와 함께 실제 프리셋(60/66/60 BPM)이 전부
  문헌 예시 대역 안으로 들어옴.
- **C10 longistat 인계 과대주장** (문서의 `--id 날짜 --time 세션` 은 중복
  (대상,시점) 오류 + 잘못된 축). → UI 문구 "statwise 에 그대로 (longistat 은
  날짜 열 추가 후)"로 수정, README·사용법 예시를
  `--id 개입여부 --time 날짜 --value 후속180초짖음수 --duplicates mean` 으로 재작성
  — 실제 longistat 에 합성 7일×2군 데이터로 돌려 종료코드 0 확인. "대상 2개
  기술적 용도, 확증은 statwise" 한계 병기.

### 경미

- **M1** tick 경계 경합: 경계에 걸친 짖음은 방금 닫힌 에피소드를 **재개**(`에피소드
  재개` 이벤트, 원장은 append-only). 새 배정 draw 없음 — 배정 수열 불변까지 테스트.
- **M2** 세션시작 더블클릭 스트림 누수: sessionActive/버튼 비활성이 getUserMedia
  await **앞**으로. 실패 시 원복. 소스 계약 테스트.
- **M3** 실행.command: `trap … EXIT` + PIPE/HUP 명시 트랩 — 비정상 종료도 서버
  고아 없음.
- **M4** detector 입력 검증: NaN/±Inf 프레임은 무시·집계(`getInvalidFrameCount`),
  보정 표본에서도 제외. 무효 프레임은 갭 판정에도 안 낀다.
- **M5** 세션 타임라인을 performance.now 앵커(단조)로 — 세션 중 OS 시계 변경이
  음수 지속시간을 못 만든다. study 는 음수 지속을 한국어 오류로 거부(방어선).
- **M6** renderPreset 의 seconds/sampleRate 무효값 한국어 오류; 에피소드 ID
  E0001(4자리 — >1000 정렬); csvCell(null/undefined)="".
- **M7** CSV 가드 접두 정규식에 탭·CR 추가 (`/^[\t\r=+\-@]/`).
- **M8** purity 의 app.js 금지 목록에 localStorage/sessionStorage/indexedDB 추가.
- **M9** 드론 AM 0.8→1.0 Hz (48→60 BPM — 문헌 예시 대역 진입).
- **M10** README "임의 시드 10종 스윕" 문구 → 스위트가 실제로 강제하는 내용
  (검증 시드 16종 전수 + 시드 3종 전지표)으로 재서술.
- **M11** 화면 요약 아래에 파일명이 채워진 statwise 명령 한 줄 노출(복사용).
- **M12** 미다운로드 CSV 를 버리는 세션 재시작에 확인 대화상자.
- **M13** engine.js 의 "코너 120 Hz" 주석 → 실제 값(300 Hz)으로 정정.
- **M14** README 대응표에 실측 β(−2.8/−4.0/−5.6)와 "핑크~브라운 서술보다 가파름
  (저역통과 캐스케이드가 회귀 상단을 깎음)" + 단측 검정 근거 명시.

### 라운드 0 자백 목록의 현재 상태

- C1(8 dB 미만 빠른 팝 맹점): **부분 해소** — ⑧ 차분 크레스트가 클릭류를 잡지만,
  6–8 dB 의 "느리지도 빠르지도 않은" 팝은 여전히 ④⑧ 사이에 있고, 라운드 2 실측으로
  사각이 더 넓음이 확인됐다: **부드러운(Hann 에지) 서브밀리초 팝은 크레스트 ~14 dB
  미만이면 ⑧도 통과**한다(500 µs·50% 진폭 팝 = 11.5/16.3 으로 미검). 엔진의
  저역통과 출력은 차분 크레스트 ≤18.3 dB 라 이런 팝을 자발 생성할 수 없어 회귀
  방어라는 목적은 유지되나, 이 테스트를 외부 오디오 검증기로 쓰면 안 된다.
- C2(융기 여유 얇음): **해소** — 정칙화 후 잡음 프리셋 융기 ~0.1, 드론 ≤0.85
  (스위트 프록시 지표 기준 — 라운드 1 C2 항목의 단서 참조), 검증 시드 전수
  스위프가 ≤0.90 을 강제.
- C3(app.js 실행 테스트 부재): **완화** — 안전 상수·가드는 소스 계약 테스트로
  고정. 실행 검증은 여전히 수동(마이크 차단 환경 한계).
- 그 외(프록시≠심리음향, 48 kHz 스모크만, mulberry32 중복, AGC 공학적 추가,
  블록화 없는 배정)는 그대로 유효한 알려진 한계다. 정칙화·AGC 등 엔진의 공학적
  추가가 늘어난 만큼 "논문의 직역"과 "구현 재량"의 경계는 README 대응표가
  행 단위로 밝힌다.
