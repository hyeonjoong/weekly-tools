# factorscan 하드닝 기록

여러 라운드에 걸친 독립 리뷰어(적대적 검토) 패널의 지적을 반영해 도구를 강화한 기록.
각 라운드는 (1) 통계 정확성(제1원리 재계산 대조) (2) 엣지케이스 (3) 실제 유용성/누락 기능
(4) 문서 정직성 (5) 테스트·보안/PII 다섯 축을 새 패널로 점검한다.

---

## 2026-07-16 — Round 1

베이스라인: 50 테스트 통과. 4개 독립 리뷰어 병렬 실행(정확성 / 엣지케이스 / 유용성 / 문서·테스트·보안).

### 검증된 강점 (변경 없음)
- **통계 코어는 정확**. `factor_analyzer`·`scipy` 독립 오라클과 기계정밀도로 일치 확인:
  Bartlett χ²/df/p, KMO 전체+문항별 MSA, `chi2_sf`(불완전감마, df 1~20000·x 0.001~50000
  상대오차 ≤~1e-13), 주성분 적재·공통성, Varimax(직교회전 공통성 보존), 평행분석 절차.
- PII/보안: 네트워크·임시파일·eval/exec 없음, 오류메시지에 원자료 미노출, 예시 CSV는 합성.
- README "실제 출력 예시"의 수치(χ², KMO, 고유값, 적재)가 실제 실행과 일치(조작 없음).

### 수정한 결함
1. **[HIGH] 설정 `scale_range` 언패킹 크래시** — `cfg["scale_range"]`가 try 밖에서 실행돼
   `[1,5,9]`/`5`/비-dict 설정이 raw 트레이스백을 냈다. → 설정 구조 검증(`_cfg_scale_range`,
   `_cfg_list`, 최상위 dict 확인) 추가, 깨끗한 `설정 파일 오류`+rc=2.
2. **[MED] CP949/EUC-KR 파일** — 한국어 엑셀 기본 인코딩이 utf-8 decode 실패로 암호같은
   오류만 냈다. → `--encoding` 옵션 추가, `UnicodeDecodeError`를 CP949 안내가 담긴 `DataError`로,
   알 수 없는 인코딩도 깔끔히 처리.
3. **[MED] 자릿수구분 쉼표("1,000")가 조용히 결측화** → `_to_float`에서 천단위 쉼표 인식·파싱.
   추가로 **숫자로 못 읽은 비어있지 않은 토큰 수**를 `Dataset.coercion`으로 세어 경고로 표면화.
4. **[LOW-MED] 헤더보다 긴 행이 조용히 잘림** → 넘치는 칸에 실제 값이 있으면 행번호와 함께 오류,
   빈 칸만 넘치면(엑셀 흔한 케이스) 무해하게 절단.
5. **[LOW] 인자 미검증** — `--min-loading -0.5`(모든 문항 교차적재로 오판)·`--scale-min≥max`
   허용 → 범위/순서 검증 후 rc=2.
6. **[LOW] 설정 items/reverse/id_cols 문자열이 문자단위 순회** → `_cfg_list`로 정규화.

### 정확성 다듬기
7. **평행분석 유지 규칙**: `np.sum(obs>ref)`(단순합산)을 **첫 교차에서 멈추는** 표준
   `retained_by_parallel`로 교체(비단조 상황 과대추정 방지).
8. **n≤p일 때 평행분석 생략**: 무작위 상관행렬이 특이해 기준선이 왜곡되는 문제 → 명시적 생략+경고.
9. **설명분산 분모**: 클리핑된 고유값 합 대신 대각합 p 사용(미세 음수 제거로 인한 총합 왜곡 제거).
10. **JSON 스키마 안정성**: `parallel_eigenvalues`/`parallel_k`를 생략 시에도 항상 `null`로 존재.

### 추가한 기능 (임상 연구 실사용 가치)
- **McDonald ω(요인별 합성신뢰도)** — 적재량으로부터 직접 계산, 표에 표시+JSON.
- **RMSR·비중복잔차(재현 상관행렬 적합도)** — 유지요인해의 적합도 근거.
- **요인(하위척도)별 수정된 문항-총점** — 다차원 척도에서 전체합 대신 소속 요인 내 합 사용
  (기존 전체합 값은 `item_total_overall`로 유지). 표시·플래그는 요인별 기준으로 전환.
- **문항별 MSA를 표에 표시**(기존엔 JSON에만 존재), **추출=주성분(PCA) 명시**를 보고서 헤더/도움말에.
- `실행.command`에 python3 부재 시 안내.

### 문서
- README "실제 출력 예시" 블록을 신규 포맷(MSA·ω·RMSR·PCA 표기)으로 갱신(실제 출력과 일치 유지).
- README/사용법.md에 `--encoding`, ω/RMSR, 요인별 문항-총점, PCA-기준 RMSR 주의 반영.

### 테스트
50 → **81 통과**. 신규 `tests/test_hardening.py`(31개): 인코딩(CP949/미지정/오류), 천단위 쉼표,
헤더 초과 행, 강제결측 카운트, 설정 검증 3종, 인자 검증 2종, 평행분석 n≤p 생략·첫교차·키존재,
요인별 문항-총점(수기 대조·싱글턴 NaN·k=1 동치), ω(공식 대조·클린데이터 범위), RMSR(완전적합 0·범위),
설명분산 합=1, report 신규항목 표시·특이행렬 MSA '—', 전체 파이프라인 결정성, 완전상관→비양정부호 경로.

신규 통계(ω·RMSR·요인별 문항-총점)는 모두 독립 수기 계산과 기계정밀도로 대조 확인.

---

## 2026-07-16 — Round 2

Round 1 산출물(신규 코드)에 새 4개 패널을 병렬 투입(정확성 / 엣지케이스 / 유용성·문서 / 테스트·보안).
핵심 지적이 여러 리뷰어에서 수렴 → 정직성·해석 위험 위주로 수정.

### 재확인된 강점
- 신규 통계 독립 재검증 통과: `retained_by_parallel`(첫 교차 규칙), `residual_stats`(비대각 RMSR),
  `corrected_item_total_by_group`, `eigen_summary`(합=1) 모두 수기/오라클과 일치.
- 보안: 천단위 정규식 ReDoS 없음(400KB 적대적 입력 <9ms), 오류메시지에 원자료 미노출,
  네트워크·eval·임시파일 없음. coercion 카운트 루프 500K행 0.5s로 선형.
- README 예시 수치(ω·RMSR·MSA 포함)가 실제 출력과 일치.

### 수정한 결함 (해석 정직성)
1. **[핵심] McDonald ω가 PCA 기반이라 낙관적인데 README 방향 설명이 반대였음** — 유용성 리뷰어가
   실측(F1 정식 congeneric ω=0.882 vs 도구 0.916)으로 상향편향 입증. 또한 정확성 리뷰어가
   "분자=자기요인 적재, 분모=전체 공통성"의 비일관성(교차적재 분산이 소실되어 과대추정)을 지적.
   → ω 공식을 **자기 요인 오차분산** `Σ(1−λ_if²)` 기준으로 일관화, docstring/보고서/README에
   **'PCA 기반 근사(낙관적), RMSR과 반대 방향'** 명시. (예시 ω 0.916/0.918 → 0.911/0.911)
2. **[핵심] ω의 <2문항 요인 미가드** — 1문항 요인이 무의미한 0.495 등을 출력. 문항-총점은 이미
   NaN 처리했는데 ω만 누락. → 요인 내 문항 2개 미만이면 `None`('—')로 정의 불가 처리.
3. **argmax 배정 불안정성 미고지** — 요인별 ω·요인총점이 |적재|최대 배정에 의존하는데 교차·저적재
   문항에서 불안정함을 알리지 않음. → 보고서/README에 참고용 경고 + 전체합 기준값(JSON
   `item_total_overall`) 병행 안내.
4. **`--rotation none` 다요인 결과 오해 유발** — 비회전 시 첫 성분에 몰려 전 문항 교차적재·F2 ω
   미정의가 아무 설명 없이 발생. → k≥2·비회전이면 안내 note 추가.

### 수정한 결함 (엣지케이스)
5. **[MED] 유럽식 소수 "1,234"(=1.234)를 자릿수구분으로 오독해 1000배 조용한 오염** —
   천단위 정규식이 단일 그룹까지 매칭. → **명확한 경우만**(소수점 동반 또는 그룹 2개 이상) 인식,
   모호한 단일 그룹은 결측+강제변환 경고로 표면화(조용한 오독 제거).
6. **[LOW] 설정 scale_range의 NaN/Infinity 통과** → `math.isfinite` 검증 추가.
7. **[LOW] `--items` 중복 지정 미거부**(파일 헤더 중복만 거부) → 중복 문항 명시적 오류.
8. **[LOW] 잘못된 --encoding으로 ID열이 조용히 분석문항으로 승격** → `--id-col` 미존재 시 경고.

### 테스트 (테스트 리뷰어 후속)
- 자기참조(tautological) ω 테스트를 **수기 리터럴**(0.7424…) 대조로 교체.
- 미보강 경로 회귀 가드 추가: config 경로 scale_min≥max·NaN·items-as-dict, CLI --json 강제변환
  경고 종단, CLI 잘못된 인코딩 rc=1, --na의 coercion 제외, ω '—' 렌더, 중복 items, 비회전 note,
  id-col 경고, **README 골든 스냅샷**(핵심 수치 표류 방지).
- 취약 임계값 테스트에 argmax 그룹핑 사전확정으로 진단성 강화.

81 → **95 통과**. 신규 통계 재검증 및 모든 Round-1/2 수정에 대응 회귀 테스트 확보.

---

## 2026-07-16 — Round 3

새 검증 패널(최종 정확성·회귀 / 문서·유용성·엣지) 투입. **결함 0건으로 클린 라운드** 확인:
- ω(자기요인 잔차) 재검증: 단일요인 congeneric ω와 정확히 일치, [0,1] 유계 증명·확인.
- 천단위 정규식 9개 스펙 케이스 전부 정확, config/인자 검증 정상, headline 통계(Bartlett·KMO·MSA·
  고유값·Varimax·공통성·RMSR·요인별 문항-총점·ω=0.911) 오라클과 정확 일치. README 예시 수치 일치.

세 라운드 모든 패널이 **일관되게 지목한 단 하나의 실질 개선점(사교회전)** 을 반영(필러 아님):

### 추가한 기능
1. **Promax 사교(oblique)회전 + 요인 상관행렬 Φ** — 임상·심리 하위척도는 대개 상관되어 직교(Varimax)
   가정이 부적절할 수 있음. `--rotation promax` 로 패턴적재+Φ 제공. `factor_analyzer`의 promax와
   패턴·Φ가 varimax 허용오차 수준으로 일치(Φ=−0.294 vs −0.293) 검증.
   - **요인 상관은 회전과 무관하게 항상 추정**해 보고서에 표시하고, |r|≥.32면 사교회전 검토 note 출력
     (직교가 타당한지 판단 근거 제공).
   - 사교회전 공통성은 `diag(P Φ Pᵀ)`(회전 불변), 설명분산은 구조행렬 기준+겹침 note로 정직하게 처리.
   - **ω는 구조행렬 S=PΦ(요인-문항 상관, |값|≤1) 기준으로 계산** — 사교회전에서 패턴계수가 1을
     넘어도 ω가 [0,1]에 머물도록 보장(직교회전이면 S=적재이므로 값 동일). 강상관 요인에서 검증.
   - promax 정규화를 Kaiser(공통성) 정규화 후 목표/최소제곱 → 복원 순서로 맞춰 factor_analyzer/R/SPSS
     promax와 자연 요인수에서 기계정밀도 일치.
   - **정확성 수정**: 사교회전 RMSR을 `R̂=PΦPᵀ`로 재현하도록 수정(초기엔 `PPᵀ`로 0.178 과대 →
     올바른 0.068). 독립 재계산으로 확인.
2. **상관행렬 행렬식(다중공선성 진단)** — det(R)<1e-5면 거의 중복 문항 경고(Field/SPSS 관례 임계).

### 문서
README 예시 블록에 요인상관 행렬 추가(실제 출력 일치), Promax·행렬식·직교/사교 한계 갱신, 사용법.md 옵션표.

95 → **105 통과**. Promax(Φ 유효성·회전불변 공통성·RMSR=PΦPᵀ·독립 알고리즘 대조·k=1 항등),
요인상관(직교하 추정·k=1 None), 행렬식·다중공선성 경고, CLI --rotation promax JSON/렌더 회귀 테스트 추가.

**결론**: 3라운드(리뷰 클린 + 최상위 요청 기능 반영) 후 통계 코어는 오라클 검증 완료, 엣지·문서·보안
모두 통과, 105 테스트 그린. 도구는 게재용 척도검증에 신뢰 가능한 상태로 판단하여 하드닝 종료.

---

## 2026-07-16 — Round 4 (심층 확장 + 4패널 병렬 검토)

베이스라인 107 테스트. **먼저 실사용 가치가 큰 신규 통계·출력 기능을 추가**하고, 이어 4개 독립 리뷰어
(정확성 / 엣지케이스 / 유용성 / 문서·테스트·보안)를 병렬 투입해 하드닝.

### 추가한 기능 (임상 척도검증 실사용 깊이)
1. **주축분해(PAF) 추출** (`--extraction paf`) — README가 유일한 한계로 명시했던 '공통요인 모형' 제공.
   SMC 초기화 → 축소상관행렬 반복분해 → 공통성 수렴. Heywood(공통성>1·경계) 절단·경고. 독립 오라클과
   공통성 1e-6 일치, ω·RMSR이 PCA보다 덜 낙관적(예시 ω 0.911→0.874, RMSR 0.068→0.036).
2. **Velicer MAP(최소평균편상관) 검정** — Kaiser·평행분석과 독립적인 제3의 요인수 근거. 편상관 기반
   평균제곱 최소화(Velicer 1976). 독립 오라클과 1e-15 일치, 잡음자료에서 0개, 클린 2요인에서 2개.
3. **요인별 Cronbach's α** — 적재기반 ω와 나란히 응답분산기반 신뢰도를 제공(추출방식 무관). 수기공식 대조.
4. **적재표 CSV 내보내기**(`--csv-out`) — 문항×요인 적재+공통성·MSA·문항총점 **+ 요약행**(SS·설명분산%·
   누적%·ω·α·요인상관 Φ)까지 담아 논문 부록 표로 완결. utf-8-sig(엑셀), 수식 인젝션 방어, 4자리 반올림.
5. **응답자별 하위척도 점수 내보내기**(`--scores-out`, `--score-method sum|mean`) — 요인구조 확인 후 곧바로
   집단비교·수렴타당도로 넘길 수 있게 결측제거 표본의 하위척도 합/평균을 ID와 함께 출력(역문항 반영).

### 4패널이 확인한 강점
- 신규 통계 4종(SMC·PAF·MAP·α) 모두 독립 오라클과 기계정밀도 일치, 결함 0. PAF 요인수 판정은 전체 R 기준.
- 문서 정직성: README 예시 수치 바이트 단위 일치, PAF/α/MAP 서술이 구현과 정확히 부합.
- 보안: 네트워크·eval·subprocess·tempfile 없음, 예시 합성, 오류메시지에 원자료 미노출.

### 수정한 결함
- **[MED-HIGH] PAF k=p 크래시** — 0 적재 요인이 promax `inv`를 특이화("Singular matrix"). `_safe_inv`(pinv
  대체)로 무해화하고, 추출 가능 공통요인 수 초과 시 명확한 안내 경고 + rc=0.
- **[LOW] PAF Heywood 경계 미탐지** — 특이자료에서 공통성이 정확히 1.0에 고정돼 '초과'가 안 나던 경우도
  경계(≈1) 플래그로 포착.
- **[LOW] CSV 리터럴 `nan`** — 적재/공통성 셀도 `_num()`으로 빈칸 처리(msa와 일관).
- **[LOW] CSV 수식 인젝션** — `=,+,-,@`·제어문자로 시작하는 셀(문항명·문제플래그)을 작은따옴표로 무력화.

### 테스트
107 → **147**. 신규 `tests/test_round4.py`: PAF(오라클·PCA대비·Heywood·k=p무크래시)·MAP(오라클·잡음0·
싱귤러None)·α(수기공식·싱글턴)·CSV(구조·반올림·인젝션·nan없음·k=1·요약행)·점수내보내기(역문항·ID정렬·
수기합·경로실패). 골든 README 스냅샷에 MAP·누적분산·α 라인 추가.

---

## 2026-07-16 — Round 5 (신규 폴리코릭·출력 기능 집중 검토)

가장 깊은 잔여 요청(순서형 처리)을 반영하고 3패널 병렬 검토(폴리코릭 정확성 / 점수·CSV / 문서·테스트·보안).

### 추가한 기능 (순서형 리커트 정공법)
6. **폴리코릭(polychoric) 상관** (`--correlation polychoric`) — 유용성 리뷰어가 방법 리뷰어의 최우선 요구로
   지목한 항목. 리커트를 잠재 이변량 정규의 구간절단으로 보고 2단계 ML로 잠재상관을 추정, 피어슨의
   하향편의를 보정. **scipy 없이** 순수 numpy+stdlib 구현: 정규 CDF(erfc)·역정규(Acklam)·이변량 정규
   CDF(Genz Gauss-Legendre)·황금분할 최적화. 추정된 상관행렬을 기존 고유값/KMO/PAF/회전 파이프라인에
   그대로 투입(엔진 재사용). 비양정부호 가능성·범주과다·비정수 입력을 경고.

### 3패널 검증 결과 (결함 0의 정확성, 2건의 견고성 수정)
- **폴리코릭 정확성**: `bvn_cdf` vs scipy `multivariate_normal.cdf` 최대오차 1e-10, 닫힌형
  Φ₂(0,0;ρ)=¼+asin(ρ)/2π 오차 3e-17, 황금분할 vs scipy `minimize_scalar` 1.6e-8 일치, 잠재 ρ 복원
  정확(0.85→0.8505), 피어슨 대비 하향편의 보정 확인, 테트라코릭·단조·희소표·소표본 엣지 모두 통과.
- **점수·CSV**: ID↔점수 정렬(결측삭제하에서도 위치정합) 정확, 역문항 반영, 요약행 수치가 사람보고서와
  일치, 인젝션 방어·반올림·라운드트립 정상, 저장 실패경로 rc=1·부분파일 없음.
- **문서·보안**: README 예시 바이트 일치, 폴리코릭 서술 정확, scipy 미의존 확인, 신규 코드에 네트워크·
  eval·subprocess·tempfile 없음.

### 수정한 결함
- **[MED] ID 없는 점수파일의 `row`가 순번(생존행 기준)** — 결측삭제 시 원본 행과 어긋나 역추적 불가.
  → `row_mask`로 **원본 CSV 행번호**를 출력하도록 수정(삭제된 행 건너뛰고 번호 보존).
- **[LOW] `open('w')` 선(先)절단** — 내용 생성 중 예외 시 기존 파일이 0바이트로 잘릴 수 있음.
  → 내용을 먼저 문자열로 만든 뒤 파일 오픈(원자적 대체 성격).
- **[LOW·문서] `--scores-out` PII 경고 부재** — 응답자 식별 자료임을 README에 명시(집계표와 구분).
- **[LOW·테스트] 점수 CSV 인젝션·폴리코릭 비양정부호 경로 회귀 가드** 추가.

### 테스트
147 → **167**. 신규 `tests/test_polychoric.py`(정규CDF/probit·닫힌형·독립성·무한경계·잠재복원·
scipy 2단계 오라클(importorskip)·대칭/단위대각·상수NaN·단조·analyze통합·비정수/과다범주 경고·비양정부호).
Round 4/5 리뷰 후속 회귀 가드(원본행번호·선절단방지·점수인젝션) 포함. 폴리코릭은 순수 numpy로
scipy 오라클과 기계정밀도 일치.

**결론**: 5라운드 누적. PAF·폴리코릭·MAP·Cronbach α·적재표/점수 CSV 등 실사용 깊이의 기능을 추가하고,
전 패널이 신규 통계를 독립 오라클로 재검증(결함 0 또는 견고성 미세수정)했다. 통계 코어는 machine-precision
검증, 엣지·문서·보안 통과, **167 테스트 그린**. 순서형 임상 리커트 척도까지 게재용으로 신뢰 가능한 상태.

---

## 2026-07-16 — Round 6: ML 적합도지수 노출 · 결측/기술통계 진단 · 엑셀 입력, 그리고 5패널 하드닝

### 배경: 죽어 있던 엔진
`efa.ml_factor_analysis`·`fit_indices`(χ²·RMSEA·CFI·TLI·AIC/BIC)·`_ml_fit_scan`이 **구현은 완료돼
있는데 CLI에서 닿을 수 없었다**(`--extraction` choices에 `ml`이 없고, `fit_scan`은 인자 자체가 없고,
report는 `fit`을 렌더링하지 않았다). 게다가 `test_analyze_invalid_extraction_errors`가 `ml`을
'잘못된 값'으로 단정해 **테스트 스위트가 red 상태**였다. 즉 이 도구의 가장 값비싼 기능이 전량 사장돼
있었다. 이번 라운드는 그것을 살리는 데서 시작했다.

### 1) 새 기능 (실사용 깊이)
- **최대우도(ML) 추출 노출** — `--extraction ml`. χ² 적합도 검정 · **RMSEA + 90% CI · PCLOSE** ·
  **CFI · TLI** · AIC/BIC를 보고서 `[3-1]`에 렌더링. 게재 표에 그대로 옮길 수 있는 형태.
- **`--fit-scan`** — k=1..식별가능 최대까지 ML을 반복 적합해 적합도 표를 만들고 **BIC 최소 k**를
  표시. 평행분석·Kaiser·MAP과 **독립적인 네 번째** 요인 수 근거. 번들 예시에서 k=2를 정확히 지목.
- **결측 구조 진단** — 문항별 결측률, listwise 손실 규모·**유발 문항**, 그리고 완전응답자 vs 삭제된
  응답자의 **Cohen's d**로 MCAR 위배 신호 점검. 깨끗한 자료에서는 표를 띄우지 않는다.
- **문항 기술통계(척도 논문 Table 1)** — 평균·SD·왜도·첨도·**바닥/천장 효과**. 바닥/천장 기준은
  **범주 수에 맞춰 조정**(5점 균등응답은 끝 범주가 20%라 15% 고정 기준이면 정상 문항까지 전부
  거짓 경보 — 실제로 개발 중 이 거짓 경보를 관측하고 `max(0.15, 1.5/C)`로 고쳤다).
  |왜도|>2·|첨도|>7이면 폴리코릭 전환을 권고 → 폴리코릭/ML 스위치가 '감'이 아니라 근거로 결정된다.
- **역문항 미처리 탐지 + 오염된 점수 거부** — 아래 '패널이 잡은 최악의 결함' 참조.
- **제거 시 α(alpha-if-deleted)** — "이 문항을 빼면 신뢰도가 오르나?"에 직접 답하는 플래그.
- **Thurstone 회귀 요인점수** — `--score-method regression`. W=R⁻¹S, 전 문항 적재를 가중치로.
  사교(promax)에서만 Φ 반영(직교의 Φ는 진단용 추정치라 가중치에 쓰면 안 된다).
- **엑셀(.xlsx)·TSV 입력** — 확장자 자동 판별, `--sheet`, `--delimiter`. zipfile+ElementTree만
  사용(openpyxl/pandas 불필요). 임상 자료는 CSV보다 엑셀로 오므로 재저장 단계를 없앴다.
- **`--eigen-out`** — 고유값 + 평행분석 기준선 CSV. 리뷰어가 이름을 대고 요구하는
  "Figure 1. Scree plot with parallel analysis"를 엑셀에서 30초에 그릴 표(matplotlib 의존 없이).

### 2) 5패널 병렬 적대적 리뷰 결과

**정확성 패널 (scipy 오라클)** — 결함 1건(경미):
- ML 목적함수는 Lawley-Maxwell 프로파일 불일치함수가 맞고 `factanal`의 `FAfn`/`FAgr`/시작값/하한과
  **동일**. 사영 BB vs `scipy L-BFGS-B`(21 시작·24 구성): 최대 `F_code − F_scipy` = **5.8e-15**.
- **"독립모형 χ² = Bartlett 통계량" 주장을 가정하지 않고 증명**: k=0에서 F=−ln|R| 이므로 두 값이
  **차이 0.0으로 정확히 일치**. χ²/df/RMSEA CI 방향/CFI/TLI/AIC/BIC 전부 오차 0.0~1.3e-11.
- 회귀 요인점수 오차 **0.0**, Cohen's d는 `ttest_ind` 역산과 **5.6e-17** 일치.
- **[LOW→수정] `p_close` 파국적 상쇄**: `1−ncx2_cdf`로 구해 참값 7e-120 자리에서 3.4e-15(부동소수
  잡음)를 냈다. → 상측꼬리를 직접 포아송 합산하는 **`stats.ncx2_sf`** 신설. scipy 대비 깊은 꼬리에서
  상대오차 **1.3e-13**(3000건 무작위 검증).

**유용성 패널 (임상 계량심리학자)** — **최악의 결함을 잡아냈다**:
- **[HIGH→수정] 역문항 미처리 → 좋은 문항을 지우게 하고, 오염된 점수를 조용히 내보냄.**
  재현 결과 α = **−1.589**(불가능한 값), 도구는 **멀쩡한 문항 2개를 '문항-총점 낮음'으로 플래그**하고
  진짜 원인은 한마디도 안 했으며, `--scores-out`은 그 문항을 **거꾸로 더한 합산점수**를 말없이 파일로
  썼다(자기 문항과 상관 **−0.346**). README가 그 파일을 "집단비교·수렴타당도 분석에 넘기라"고
  권하고 있었으므로, 잘못된 논문 결과로 직행하는 경로였다.
  → ① 주적재 음수 문항을 찾아 **실행할 명령(`--reverse Q3 --scale-min 1 --scale-max 5`)까지 제시**,
    ② sum/mean 점수는 **파일을 쓰지 않고 거부**(rc=1, 부분 파일 없음), 회귀점수는 부호를 가중치가
    품으므로 허용. 선언 후 α가 −1.589 → 0.894로 정상화됨을 회귀 테스트로 고정.
- **[MED→수정] 작은 표본의 거짓 안심**: n=60·p=10은 문항당 6명이라 비율 규칙(n<5p)을 통과해
  **아무 경고도 없었다**. → 절대 표본 경고(n<150) 추가(MacCallum et al. 1999: 적정성은 인원비가
  아니라 절대 n·공통성에 달렸다). **χ² 비유의를 '적합'이라 쓰지 않도록** 수정(n<200이면 검정력 부족
  명시), **RMSEA 판정을 점추정이 아니라 90% CI로** 읽도록 변경(CI가 .05~.10을 가로지르면 '결론 보류'),
  **TLI>1은 우수가 아니라 과다모수화 신호**임을 명시.
- **[MED→수정] PCA 적재로 계산한 ω를 'McDonald ω'라 표기** — 각주 고지는 잘못된 라벨을 취소하지
  못한다(그 문자열이 그대로 논문에 복사된다). → PCA에서는 **`합성신뢰도(ω, PCA 근사·낙관적)`** 로
  이름 자체를 변경.
- 패널이 **반려를 권고한 것**(수용): 자동 문항정제 루프(단일표본 우연자본화), 측정동일성/이차·bifactor
  (lavaan의 영역, 잘못된 결과=논문 철회), Empirical Kaiser(기준만 늘어 마비). 스코프를 지켰다.

**엣지 패널** — 조용한 오답 4건 수정:
- **[MED-HIGH] 자동선택에서 열이 통째로 사라지는데 경고 0건** — "1,234"(모호한 자릿수 쉼표), 캐시
  없는 수식 셀, 전부 결측 열이 말없이 빠졌다. 코드 주석이 "조용한 1000배 오독을 막는다"고 약속해
  놓고 정작 자동선택 경로에서 경고가 안 났다. → `Dataset.dropped`(열→사유)로 수집해 보고.
- **[MED-HIGH] 엑셀 날짜 셀이 일련번호로 분석에 들어감** — `검사일` 열이 43831 같은 값이라 분산도
  있고 결측도 없어 **리커트 문항인 척 요인분석에 섞였다**. → `styles.xml`의 numFmt를 파싱해 날짜
  서식을 인식하고 ISO 문자열로 변환(1900 가짜 윤일·1904 체계 포함).
- **[MED] `r` 속성 없는 셀이 다른 열을 덮어씀**(ECMA-376에서 `r`은 선택) → 진행 커서로 수정.
- **[MED] 한중일 엑셀의 음성 안내(`<rPh>` 루비)가 열 이름에 섞임**('Q1'→'Q1PHONETIC') → 직접 자식과
  `<r>`만 읽도록 수정. **한국어 사용자에게 직결되는 버그였다.**
- 그 외: `--delimiter '\t'` TypeError 역추적 → 탭으로 해석 + 한 글자 검증, listwise 전멸 시 "표본이
  적다"는 엉뚱한 메시지 → 진짜 원인·유발 문항 안내, `--fit-scan`이 p≤3에서 침묵 → 경고, 1e308 값의
  numpy 영문 경고 폭포 → 한국어 오류로 선제 차단.

**보안/테스트 패널** — HIGH 3건 수정:
- **[HIGH] `_col_index` 무제한 → 3.3KB 파일로 RSS 1.05GB.** `ch.isalpha()`가 한글·CJK 확장 문자까지
  자릿수로 받아 `𪘀1` → 열 195036 → 행 하나를 20만 칸으로 조밀 전개. 압축비 상한으로는 못 막는다
  (**압축 해제된 XML 대비** 5만 배 증폭). → ASCII만 인정 + XFD(16384) 상한 + 잘못된 참조 거부.
  **재현 확인: 1.05GB/4.3s → 0.03GB/0.01s.**
- **[HIGH] ML 목적함수에 독립 검증이 없었다** — 패널이 `_ml_objective`에 ×1.05를 곱하는 변이를
  넣자 **스위트가 그대로 초록**이었다. χ²·RMSEA·CFI·TLI·AIC/BIC·fit-scan 권고가 전부 F_min에
  비례하므로 **논문에 실리는 수치 전체가 무방비**였다. 기존 테스트가 순환적이었다(목적함수 자신을
  불러 기대값을 만들거나, `ml.criterion`을 입력으로 받거나, 생성자 한 줄을 그대로 복창).
  → 정의식 `F = tr(RΣ⁻¹) − ln|RΣ⁻¹| − p`(구현의 고유값 형태와 완전히 다른 식)로 재계산하는 오라클
  테스트 추가. **변이 재실행 결과 정상 검출.**
- **[HIGH] 엑셀 테스트가 openpyxl 방언만 검증** — openpyxl은 `t="inlineStr"`로 쓰지만 **진짜 엑셀은
  `sharedStrings`+`t="s"`**를 쓴다. 즉 실제 임상 .xlsx가 타는 경로가 미검증이었고, openpyxl이 없는
  환경에서는 **8건이 조용히 skip**되며 exit 0이었다. → 픽스처를 **원시 XML로 직접 생성**해 두 방언을
  모두 시험(서드파티 의존 제거, skip 0건).
- **[MED] CSV 인젝션은 `=`만 시험** — `_safe_text`를 `=`만 막도록 좁히는 변이가 스위트를 통과했다.
  → 7개 접두문자 × 3경로(문항명·ID열이름·ID값) 파라미터화. **변이 재실행 시 18건 실패로 검출.**
- **[MED] zip 멤버 크기 무제한**(2MB → 2GiB 팽창, `OverflowError` 미포착) → 256MB 상한 + 포착.
- 패널이 **안전을 확인**한 것: zip 경로 탈출(extract 미사용), XXE·billion-laughs(libexpat 2.7.3에서
  미해석 — 다만 이는 환경 덕이지 코드 덕이 아니라는 지적을 받아들여 회귀 테스트로 고정), 코드 실행/
  네트워크 없음, PII가 경고·오류·JSON에 새지 않음(전부 집계값·열이름만), `--scores-out` PII 경고 존재.

**문서 패널** — README/사용법의 모든 명령을 실제 실행, 예시 블록 **바이트 단위 대조**:
- 두 예시 블록 모두 실출력과 **정확히 일치**(이후 이번 변경으로 재생성 → 재검증 완료).
- **[MED→수정]** 결측 편향 점검의 건너뛰기 조건을 잘못 기술("삭제군이 전부 결측이면"이 아니라
  **양 군 중 하나라도 유효응답 5명 미만**). 번들 예시는 삭제 3명이라 이 기능이 **애초에 표시되지
  않는다**는 사실까지 명시.
- **[MED→수정]** "`--fit-scan`이 df≤0인 k를 표에 표시한다"는 거짓(애초에 루프 범위 밖) → 정정.
- **[MED→수정]** ML 국소최소 미공개 — 독립 오라클이 무작위 자료의 **18.2%**에서 최적해가 아닌
  정류점에 멈춤을 확인(단일 시작 L-BFGS-B는 12.8% — 최적화기가 아니라 목적함수의 다봉성).
  `factanal`도 동일하며 R은 `nstart`로 완화한다는 점과 함께 한계에 명시.
- **[LOW→수정]** `_factor_correlation` 행이 varimax에서도 나가는데 라벨이 없어 직교 부록 표에
  Φ=0.29가 함께 실릴 수 있었다 → `_factor_correlation(promax_estimate;varimax해는Φ=I)`로 개명.
- "numpy만 의존"·"scipy 불필요" 주장은 **사실 확인**(shipped 코드 전 import 검사). scipy는 테스트
  전용 오라클이므로 `[test]` extra에 명시(폴리코릭 오라클 2건의 조용한 skip 제거).

### 테스트
167(1 red) → **286 green**. 신규 `tests/test_round6.py`: ML 정의식 오라클·적합도지수 1원칙 재계산·
RMSEA CI 방향·fit-scan 진짜 k 지목·`ncx2_sf` 깊은 꼬리·결측 진단(d 재계산)·회귀점수 Thurstone
공식·엑셀 두 방언/날짜/루비/r-없는 셀/증폭공격/압축폭탄/XXE·역문항 탐지와 점수 거부·기술통계 적률·
범주별 바닥천장 기준·alpha-if-deleted·절대 표본 경고·스크리 CSV·인젝션 7×3 파라미터화.

**결론**: 사장돼 있던 ML 엔진을 살려 요인 수 결정에 네 번째 독립 근거를 주고, 임상 엑셀을 그대로 먹으며,
"무엇을 잃었고 무엇이 못 미더운지"를 숨기지 않게 만들었다. 무엇보다 **역문항 미처리로 오염된 점수를
말없이 내보내던 경로**와 **논문에 실리는 모든 적합도지수가 무검증이던 구멍**을 닫았다. 통계 코어는
scipy·factanal 관례와 기계정밀도 일치, 변이 검증으로 테스트의 실효성까지 확인.
