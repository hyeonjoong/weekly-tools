"""robustcheck — 이미 나온 결론이 분석 선택을 바꿔도 살아남는지만 본다.

이 패키지가 지키는 두 가지 불변식(나머지를 전부 빼도 이 둘은 남는다):

1. **커버리지 자백 없이는 리포트를 내보내지 않는다.**
   몇 개 시나리오를 실제로 계산했고 몇 개를 왜 건너뛰었는지 밝히지 않은
   리포트는 `report.ReportIntegrityError` 로 막힌다. 조용한 절삭 금지.

2. **"가장 유의한 조합"을 절대 추천하지 않는다.**
   시나리오 정렬 기준은 *뒤집힘 여부*이지 유의성이 아니다. 이건 편의가
   아니라 윤리 문제다 — 정렬 기준 하나만 잘못 잡으면 이 툴은 p-해킹
   자동화 도구가 된다. `verdict.order_key` 와 그 테스트가 이걸 강제한다.

이 툴은 **새 결론을 만들지 않는다.** 주 분석은 사용자가 명시해야 하고
(`--design`/`--value`/`--group` 등이 없으면 exit 2), 기준선 통계량은
리포트에 딱 한 줄만 인쇄된다. 나머지는 전부 그 한 줄 대비 *변화*다.
검정을 골라 주는 일은 `statwise` 의 몫이다.
"""

__version__ = "0.1.0"

VERSION = __version__

__all__ = ["__version__", "VERSION"]
