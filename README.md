# weekly-tools — 주간 자동 제작 툴 모음

매주 드문 드문 유용한 툴을 하나씩 만들어 이 저장소에 하위 폴더로 추가·커밋합니다.
같은 폴더가 내 컴퓨터(`~/Downloads/02_프로젝트/깃헙/`)에 그대로 쌓여 바로 실행할 수 있습니다.

- 폴더명은 `이름-역할` 규칙 — 한눈에 뭐하는 툴인지 보입니다.
- 각 폴더의 `실행.command`를 더블클릭하면 바로 동작을 볼 수 있고, `사용법.md`에 한글 안내가 있습니다.

## 설치된 툴

| 툴 (폴더) | 한 줄 설명 | 바로 실행 | 추가일 |
|----------|-----------|----------|--------|
| [stimaudit-자극세트점검](stimaudit-자극세트점검) | 실험에 쓸 **소리 파일 여러 개**를 한꺼번에 읽어 **세트로** 판정합니다 — 파일 하나에 점수를 매기지 않고 **파일들 사이**만 봅니다. 답하는 질문은 하나입니다: *"이 소리들이 서로 비교 가능한 세트인가, 그리고 설계서가 주장한 대로 만들어졌는가?"* 4조건 비교에서 활성 조건이 대조군보다 크면 **"효과가 있다"는 결론은 "소리가 더 컸다"와 구분되지 않습니다** — 리뷰어가 반드시 묻고, 데이터를 다 모은 뒤에는 고칠 방법이 없습니다. 그런데 지금 이걸 확인하는 방법은 대개 (a) 사운드 담당자가 맞췄겠거니 하거나 (b) Audacity 로 파형을 눈으로 보는 것인데, **파형 높이는 피크이지 음량이 아닙니다**(핑크노이즈와 드론은 피크가 같아도 체감 음량이 6 dB 넘게 차이 납니다). 빌드 전 실측이 그 가설을 확인했습니다: RESONATE 4조건의 `S1_SO-CLAS` 와 대조군 `S3_pink` 는 **11.6 LU** 차이였고 S1 은 트루피크 +1.5 dBFS 로 잘려 있었습니다. **자체 구현 ITU-R BS.1770-4 통합 라우드니스**(K-weighting + 게이팅)를 실물 12개에서 `ffmpeg -af ebur128` 과 대조해 **최대 편차 0.042 LU · 트루피크 0.067 dB**, **IEC 61672 A-weighting** 은 31.5 Hz–4 kHz 규격값과 0.13 dB 이내 — 전부 표준 라이브러리(24bit 3바이트 부호확장·radix-2 FFT·쌍선형 변환까지 직접). 조건 간 LUFS 차이 행렬(경고 1.0 LU · 치명 2.0 LU)과 **논문 단위(LAeq·LAmax·다이내믹 레인지)를 항상 나란히** 인쇄하고, 파일 위생(클리핑 구간·DC·앞뒤 무음·시작 클릭·좌우 불균형·죽은 파일/죽은 채널), 설계 JSON 의 주장(`carrier_hz`·`beat_hz`·`mod_hz`·`duration_s`)이 **실제 신호에 있는지** 대조(실물 싱잉볼 파일에서 L 359.9 / R 400.0 → 맥놀이 40.01 Hz 확인), 버전 대조(`--baseline`), DEBUSSY 매니페스트로 교란 후보 비교, 그리고 Methods 에 붙일 **자극 기술표 + KR/EN 문단 초안**까지 냅니다. 가장 공들인 부분은 **말하지 않는 것**: **논문 수치를 임계값으로 쓰지 않습니다**(참조값 자료형에 심각도 필드가 아예 없어 구조적으로 등급을 붙일 수 없고, `tier1_compliant` 같은 불리언은 코드·CSV 어디에도 없습니다 — 저자 자신이 1st revision 에서 철회한 프레이밍을 사내 툴로 되살리지 않기 위해, 테스트로 강제). **심리음향량(asper·acum)은 자체 계산하지 않고**(AST 검사로 강제) `--manifest` 로 받거나 "검사 안 함"으로 자백하며, **커버리지 자백 블록이 없으면 리포트 자체를 출력하지 않습니다**(코드로 강제). 파일이 1개면 아무 판정 없이 **exit 2** 로 DEBUSSY/`bell_acoustic_qc.py` 를 가리키고, 못 읽은 파일이 하나라도 있으면 비율로 봐주지 않고 **exit 3**(1보다 우선 — 다 못 들었으면 "치명 0건"은 거짓말이므로), 절대 SPL 요구는 이유를 설명하고 거절합니다 (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 546개 테스트, 적대적 서브에이전트 2라운드가 찾은 결함 20여 건 수정 — **파일 전체가 클리핑된 자극이 "클리핑 0건"으로 통과하던 사고**, 핑크노이즈 대조군에 **가짜 반송주파수 5.38 Hz** 가 붙던 사고, NaN 한 샘플이 조건 간 판정을 조용히 끄던 사고, 산출물이 `--manifest` **입력 파일을 덮어써 없애던** 사고, 파일 이름의 개행으로 **가짜 `[치명]` 줄을 리포트에 심을 수 있던** 사고, cp949 콘솔에서 인코딩 오류가 **"치명 발견"(종료코드 1)으로 보고되던** 사고, 그리고 **논문 원문 대조로 잡은 인용 오류**(Czempik 2020 의 LAeq20sec 상관은 −0.50 이 아니라 **−0.41**, 57.9 dB 는 exemplar 가 아니라 **ROC 절단점**) 포함 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `stimaudit 자극폴더/*.wav --inspect` → `--emit-design > 설계.json` → `stimaudit 자극폴더/*.wav --design 설계.json --out-dir 자극점검_202608` | 2026-08-28 (금) |
| [circadia-일주기리듬분석](circadia-일주기리듬분석) | 워치 export CSV(심박·걸음·수면구간, Apple 건강/삼성헬스/Fitbit식 열 자동 인식) → **일주기리듬 정량화 + 한국어 건강정보 리포트**: 코사이너(MESOR·진폭·정점위상, zero-amplitude F검정)·비모수 지표(IS 일간안정성·IV·RA·L5/M10, Van Someren)·**SRI 수면규칙성**(Phillips 2017)·사회적 시차(Roenneberg)·심박 야간강하율·**48시간 더블플롯 텍스트 액토그램**. 모든 참고범위는 **원문 대조 인용 + 판정 경계 명시**("참고범위 대비 낮음/높음"까지만 — 진단 문구는 금지어 테스트로 원천 차단), 유효 5일 미만이면 지표 계산 거부·착용률 미달 exit 3(조용히 틀린 값 금지). 지표.csv는 statwise 연계(실제 실행 검증). BELL-001 SERENE 워치 데이터의 탐색적 일주기 지표 파이프라인 겸용 (외부 의존성 0, 네트워크 0, 입력 읽기 전용 — 심볼릭·하드링크 덮어쓰기 차단, 테스트 112개, 적대 패널 2라운드·리뷰어 5명이 찾은 결함 25건 수정, 수학 코어는 독립 재계산 전수 일치 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `circadia 심박.csv --steps 걸음.csv --sleep 수면.csv --out-dir 리듬결과` | 2026-08-27 (목) |
| [deidaudit-비식별화점검](deidaudit-비식별화점검) | 피험자 단위 CSV/TSV/XLSX 를 **밖으로 내보내기 직전에 전 셀 스캔**해서 "이 파일을 보내도 되는가" 하나에만 답합니다. 카탈로그의 25개 툴 중 **아무도 묻지 않던 질문**입니다 — `joinaudit` 은 합치고, `visitaudit` 은 프로토콜 준수를 보고, `tracecheck` 는 원고 숫자를 봅니다. 금요일 오후 "그 데이터 좀 보내주세요"에 엑셀로 열어 이름·전화 열을 지우고 첨부하면 5분이면 끝나지만, **지우지 못한 것**이 남습니다: 수면일기 `비고` 열 187행의 "○○○ 간호사한테 얘기함", 자유응답에 본인 번호를 적은 피험자 1명, 방문 날짜 열 전체(병원 기록과 대조하면 특정됨), 그리고 **XLSX 안에 숨겨진 시트의 원본 명단** — 어느 것도 파일을 열어봐서는 구조적으로 보이지 않습니다. 직접식별자 8종(주민등록번호는 **체크섬을 실제로 계산**, 통과=치명·불일치=경고 / 휴대전화 / 유선·070·+82 / 이메일 / 한글 성명 / 생년월일 / 89세 초과 / 주소 열), 자유텍스트 인명, 엑셀 **숨김 시트·숨김 열/행·셀 주석·`docProps` 작성자·정의된 이름**, 그리고 가명화해도 남는 **재식별 위험 k**(동치류 크기 + "어느 열을 빼면 k≥5")를 파일·시트·행·열까지 찍습니다. 반복측정에서 같은 사람의 12개 행을 "동치류 12"로 세어 안전해 보이게 만드는 함정을 피해 **행이 아니라 사람 수**로 셉니다. 요청하면 가명화·날짜이동 사본을 만들고 **그 사본을 처음부터 다시 감사**해 종료코드를 냅니다 — 날짜 이동은 피험자 내 간격·자정 넘김 야간 귀속·(옵션)요일이 보존되는지 **독립 재계산으로 검증**하고 하나라도 깨지면 내보내기를 통째로 취소합니다. 가장 공들인 부분은 **울지 않는 것**과 **툴 자신이 유출 경로가 되지 않는 것**: 이미 가명화된 실제 스타일 파일에서 경고 33건이 나오던 것을 0건으로 좁혔고(`연구간호사 확인함` 이 매 행마다 이름으로 잡히던 사고 — 역할 수식어 60여 개가 전부 한국 성씨로 시작합니다), 리포트는 행 번호가 사본과 1:1 로 맞아 함께 보내면 가명이 풀리므로 **내보낼 폴더 밖**(`_점검리포트/`, 0600)에 쓰고, 키 파일은 대소문자·유니코드 정규화·심볼릭/하드링크까지 막아 `--out-dir` 밖으로 강제합니다. 증거는 언제나 마스킹(`010-****-**89`·`김○○`·`…○○○ 간호사…`)이고 `재식별위험.csv` 에는 동치류 **크기만** 씁니다. **읽지 못한 파일이 하나라도 있으면 비율로 봐주지 않고 종료코드 3** — 다 못 봤으면 "치명 0건"은 거짓말이기 때문입니다. 파일을 합치라고 하면 `joinaudit` 을 가리키며 exit 2, 통계는 한 줄도 계산하지 않습니다 (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 279개 테스트, 적대적 서브에이전트 6명이 2라운드로 찾은 결함 60여 건 수정 — 셀 안의 `|` 때문에 표가 1열로 뭉개져 이름 12건이 미검사인 채 exit 0 이던 사고, `검사율 100.0%`가 상수였던 사고, 자체검증을 통과하는 **잘못된 변환 4종**, macOS 대소문자·NFD·하드링크·심볼릭 링크 하위폴더로 키 파일이 내보내기 폴더에 떨어지던 사고, 시트 이름이 그대로 보낼 폴더의 **파일 이름**이 되던 사고, 값 하나로 6KB 파일이 2GB 를 먹던 사고, 날짜를 하나도 안 옮기고도 "이동되었다"고 쓰던 Data Availability 문장, 그리고 **1라운드 수정이 서로를 깨뜨려 되살아난 '조용한 전원 통과' 7건**(헤더 찾기가 구분자 추정과 밀린-셀 스캔을 되돌려 놓음) 포함 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `deidaudit 내보낼파일.csv 또다른.xlsx --quasi birth,sex,visit_date` (사본은 `--pseudonymize --shift-dates --shift-weeks --out-dir 보낼폴더 --key-out ~/보안/키.csv`) | 2026-08-27 (목) |
| [calmbark-반려견안정사운드](calmbark-반려견안정사운드) | 반려견이 짖으면 마이크로 감지해 **NBR 사운드 논문(1st revision, Table 1) 티어 규칙대로 합성한 저각성 안정 사운드를 자동 재생**하는 완전 오프라인 브라우저 앱 — 카탈로그 최초의 **데이터 수집 도구**이자 논문 지식이 코드가 된 첫 사례. 논문 준수가 장식이 아니라 **회귀 테스트 8종으로 하드코딩**됩니다(스펙트럼 기울기 β<0 · 30–150 Hz 러프니스 대역 회피(Arnal 2015 편도체 AM 대역) · 변조 42–80 BPM · 온셋 ≥50 ms · 다이내믹레인지 ≤15 dB · 4 kHz 초과 ≤5% · 루프 주기성 · 클릭/크레스트 가드 — 음성 대조 8종이 각 지표의 판별력을 증명, **검증 시드 16종 전수 스위프**). 효과를 주장하지 않고 **측정합니다**: 관찰/상시/**무작위 개입**(에피소드 단위 50% 배정, seed 재현) 3모드로 짖음 에피소드를 CSV 기록 → `statwise` 로 "우리 개한테 진짜 효과 있나" N-of-1 검정. 무작위 모드의 대조군 오염(재생이 에피소드 경계를 넘는 문제)은 **재생겹침초·고정 180초 창 짖음수 열**로 통제하고 분석 가이드에 명시. 마이크 오디오는 스칼라 dB 값 하나만 유출(원본 무저장·무전송), 네트워크 0(CDN 포함 — 테스트로 강제), 사운드 전부 합성(파일 0), 볼륨 하드캡+동물 청각 경고 상시. 음소거 보정 거부·마이크 사망 감지·절전 갭 폐기 등 무인 감시의 조용한 실패를 전부 막음 (테스트 150개, 적대 패널 2라운드·리뷰어 5명이 찾은 결함 27건 수정, 엔진은 시드 안정성 위해 영위상 정규화로 재설계 — `HARDENING.md`) | `실행.command` 더블클릭(localhost 서버 자동) · 또는 [HF Space](https://huggingface.co/spaces/jjjooong/calmbark)에서 폰으로 바로 | 2026-08-21 (금) |
| [robustcheck-결과견고성점검](robustcheck-결과견고성점검) | 분석용 CSV 한 장 + **이미 정해 둔 주(主) 분석 하나** → **그 결론이 분석 선택을 바꿔도 살아남는지 수십~수백 번 재계산해 '뒤집히는 조합'만** 찍어 줍니다. 저장소의 통계 엔진 9개는 전부 **결론을 한 번 만들어 내는** 도구입니다 — `statwise` 는 검정을 골라 한 번 돌리고, `longistat` 은 추이 모형을 한 번 적합합니다. **그 결론이 얼마나 안정적인지 묻는 툴은 없었습니다.** 피험자를 한 명씩 빼고(leave-one-out 전수), 이상치 규칙 3종(없음·±3SD·IQR 1.5배), 결측 처리 3종(완결자만·LOCF·평균대체, 전후 설계), 모수↔비모수(Welch↔Mann-Whitney, 대응 t↔Wilcoxon, Pearson↔Spearman, ANCOVA↔Quade 순위 ANCOVA), 로그변환 유무를 교차해 `paired` 36개 / `two-group`·`corr` 12개 명세를 전수 재계산합니다. **N=34면 손으로 600회가 넘어 사람은 못 하는 일**이고, 안 하면 4개월 뒤 *"the reported effect is not driven by a small number of participants"* 라는 Reviewer 2 코멘트로 돌아옵니다. 가장 공들인 부분은 **울지 않는 것** — ③④(효과크기 부호·등급 변화)를 세 겹으로 좁혀 ①양쪽 다 비유의면 아예 보지 않고(귀무 결과에 '취약'을 뱉으면 두 번 다시 안 열립니다: 실측 40건 중 39건 → 6건) ②로그 척도의 g 와 원척도 g 는 등급 비교조차 하지 않으며 ③경계를 스치는 변화(0.499→0.501)는 세지 않습니다. **비모수 검정이 원리적으로 alpha 에 도달할 수 없는 소표본**(n=3/3 이면 최소 p=.100)도 뒤집힘으로 세지 않고 사유를 남깁니다. **'가장 유의한 조합'을 절대 추천하지 않습니다** — 정렬 기준이 유의성이 되는 순간 이 툴은 p-해킹 자동화 도구가 되므로, 정렬은 *뒤집힘 여부* 순이고 기준선이 비유의일 때는 ② 목록을 **개수만** 알립니다(값은 CSV 로만). leave-one-out 은 주 시나리오와 *뒤집힌* 시나리오에만 곱하고 그 규칙을 리포트에 인쇄하며, **커버리지 자백 블록이 없으면 리포트 자체를 출력하지 않습니다**(코드로 강제). 주 분석을 명시하지 않으면 **아무 판정도 하지 않고 exit 2**(statwise 재탕 방지), 유효 N<6 이거나 계산된 시나리오<5 면 뒤집힘을 세지 않고 **exit 3**(1보다 우선). `joinaudit` 의 `merged.csv` 를 그대로 받고 ISI 처럼 로그가 말이 안 되는 지표는 `--no-log` 로 축을 빼며, 논문·리비전 응답서에 붙일 **KR/EN 민감도 분석 문단 초안**까지 냅니다 (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 638개 테스트, scipy 대비 p값 ≤1e-9, 적대적 서브에이전트 6명이 2라운드로 찾은 결함 40여 건 수정 — **귀무 결과 40건 중 39건에 '취약'을 뱉던 사고**, 1e308 에서 크래시하며 종료코드 1(취약)을 내던 사고, `--out-dir` 밖으로 환자 ID 를 쓸 수 있던 심볼릭 링크 취약점, NFD 한글 때문에 **한 군이 자기 자신과 비교되던** 사고, 유럽식 소수점 `3,14` 를 314 로 읽던 사고, 그리고 **내가 넣은 수정이 새로 만든 결함 6건**(정확분포 하한의 오버플로·off-by-one·오적용) 포함 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `robustcheck 데이터.csv --design two-group --group arm --value isi_week4 --out-dir 결과` | 2026-08-21 (금) |
| [tracecheck-원고수치출처추적](tracecheck-원고수치출처추적) | 원고(.docx/.md/.tex/.txt) + **분석 출력 파일 묶음**(CSV/TSV/JSON/XLSX/MD/TXT 폴더) → **Abstract·Results·표·캡션에 적힌 숫자가 실제로 그 출력 안에 있는지 전수 대조**. 저장소 전체가 검사하지 않던 단 하나의 화살표 — "분석 → (사람의 손) → 원고" — 를 봅니다. `numcheck` 는 **원고 밖을 보지 않아** 옛 분석 결과가 내부적으로 정합적이기만 하면 GRIM 도 p 재계산도 전부 통과시킵니다(그 숫자들은 *한때 진짜였기* 때문). tracecheck 는 **어느 출력에도 없는 숫자**(출처 불명)와 **이전 번들에만 있는 숫자**(재분석 후 갱신 누락 — 데이터 락 뒤 초록에 옛 평균 하나가 남는, 리뷰어도 에디터도 못 잡는 그 사고)를 **파일·행·열까지 찍어서** 잡습니다. 반올림은 half-up/half-even/half-down/부동소수를 **모두 허용**해 오탐을 막고, `8주`·`p<0.05`·`95% CI`·`[3]`·`Table 2`·`PHQ-9`·`1:1`·NCT 번호 등 **12종을 사유별로 세어 자백**하며, **커버리지 자백 블록이 없으면 리포트 자체를 출력하지 않습니다**(코드로 강제). 미매칭율이 30% 를 넘으면 치명 200건을 쏟는 대신 **종료코드 3 으로 멈춰** "이 번들이 이 원고의 분석이 아닌 것 같다"고 말하고, `--outputs` 없이는 **아무 판정도 하지 않고 exit 2**(numcheck 재탕 방지). 부호만 다른 값(`14.6분 감소` ↔ `-14.63`)과 라벨 문자열 매칭(`phq9_change` 의 9)은 치명이 아니라 **경고**로 내려 실제 원고에서 거짓 치명이 쏟아지지 않게 했습니다(적대적 검토 실측: 18건 → 1건). 산술은 재계산하지 않고(그건 `numcheck`), 값의 **의미는 추정하지 않습니다** (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 260개 테스트, 적대적 서브에이전트 7명이 2라운드로 찾은 결함 40여 건 수정 — 지수 표기 `1.5e-05` 가 유령 숫자 둘로 쪼개지던 사고, CSV 따옴표 안 줄바꿈으로 값이 조작되던 사고, UTF-16 으로 숨긴 XML 엔티티 폭탄, 1MB 한 줄 원고가 사실상 멈추던 사고, **내가 넣은 수정이 킬러 기능을 죽이던 회귀** 포함 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `tracecheck 원고.docx --outputs 분석출력_2026-08-18/ --previous 분석출력_2026-08-03/ --out-dir 출처대조결과` | 2026-08-20 (목) |
| [visitaudit-방문일정이탈점검](visitaudit-방문일정이탈점검) | 피험자별 방문 날짜 CSV + **프로토콜(방문창) JSON** → **"이 시험이 계획대로 굴러가고 있는가"를 날짜 산술로 전수 대조**: 방문창 이탈(예정일 ± 창, 경계 포함, 며칠 밖인지 부호까지)·순서 위반·필수방문 결측·중복 행·불가능 날짜, 선정/제외기준 재점검, **CONSORT 흐름 숫자**(정합성 검사 포함)·**PP 집합 후보**(사유별, 중복 인원 제거)·월별 등록 속도와 목표 도달 예상, 그리고 논문 Methods 에 붙일 **KR/EN 문장 초안**. 유효성 데이터는 한 줄도 필요 없어 **등록이 진행 중인 지금**(SERENE) 쓸 수 있는 유일한 툴이고, 같은 규칙으로 뽑아두면 모니터링 회의 숫자와 논문 CONSORT 숫자가 어긋나지 않는다. 가장 공들인 부분은 **세지 않는 것** — 창이 아직 안 닫힌 방문(미도래)과 탈락 이후 방문(해당없음)은 **절대 이탈로 세지 않고**(안 그러면 첫 실행에 "이탈 37건"이 떠 두 번 다시 안 열린다), 중복 행·깨진 날짜·기준방문 결측은 추측 대신 **판정불가**로 자백하며, 판정률이 임계 미만이면 조용히 통과시키지 않고 **종료코드 3**. `--as-of` 를 리포트와 모든 CSV 에 박아 **같은 입력 → 항상 같은 숫자**. **프로토콜 JSON 없이는 아무 판정도 하지 않고 exit 2**(joinaudit 재탕 방지). joinaudit 의 `merged.csv` 를 그대로 입력으로 받음 (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 305개 테스트, 적대적 서브에이전트 10명이 3라운드로 찾은 결함 50여 건 수정 — 심볼릭 링크·대소문자·유니코드 정규화·하드링크로 **입력 파일이 산출물에 덮여 쓰이던 사고**(첫 수정이 불충분해 두 번 고침), `nan` 이 선정기준 위반으로 둔갑하던 사고, 논문 문장의 산술이 안 맞던 사고 포함 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `visitaudit 방문기록.csv --protocol 프로토콜.json --subjects 피험자.csv --as-of 2026-08-20 --out-dir 결과_202608` | 2026-08-20 (목) |
| [revcheck-리비전응답점검](revcheck-리비전응답점검) | **제출본 · 개정본 · 응답서(point-by-point) 세 파일을 동시에 읽어**, 응답서에 적은 약속이 개정 원고에 실제로 반영됐는지 대조: **리뷰어 코멘트 번호 전수 점검**(2-4 다음이 2-6이면 치명 — 사람은 '없는 번호'를 못 본다), **응답서가 인용한 '개정 후 문구'가 개정본에 문자 그대로 있는가**(없으면 치명 + 가장 가까운 문장을 일치율과 나란히; 어긋난 숫자가 있으면 일치율 99%여도 치명, 축약 인용은 경고), **응답서에 연결되지 않은 채 있던 값이 바뀐 문단**(치명 — 숫자가 덧붙기만 한 것은 아님), 제출본 오첨부 사고, 검증 불가한 변경 주장, 위치 참조(`.docx` 는 줄 번호가 없어 **확인불가로 정직하게 강등**), 참고문헌·그림·표 증감. `리비전점검.md`/`문제목록.csv`/`변경목록.csv`/**`추가문헌.csv`(citecheck 입력 스키마 그대로)**. 변경내용 추적이 켜진 워드 파일은 **어느 상태로 읽었는지 첫 줄에 명시**하고, 코멘트 번호 체계를 못 잡으면 추측 대신 **종료코드 3(판정불가)** 으로 멈춤. 커버리지 자백 필수 출력. `numcheck`(숫자)·`draftcheck`(형식)·`citecheck`(DOI)가 원고 **1개**를 투고 직전에 보는 3종 세트라면, revcheck 는 원고 **3개**를 리비전 직전에 본다 (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 204개 테스트, 적대적 서브에이전트 7명이 2라운드로 찾은 결함 40여 건 수정 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `revcheck --old 제출본.docx --new 개정본.docx --response 응답서.docx --out-dir 결과` | 2026-08-14 (금) |
| [numcheck-원고수치검증](numcheck-원고수치검증) | 원고(.docx/.md/.tex/.txt) 한 개 → **본문에 적힌 숫자를 전부 다시 계산해 대조**: 비율(`23/48 (45.2%)` → 47.92%), 보고된 검정통계량에서 나오는 p(자체 구현 t/F/χ²/z/r CDF, scipy 대비 ≤1e-9), 하위군 N 합계, **GRIM/GRIMMER**(N = 23 에서 ISI 평균 14.37 은 존재할 수 없음 — SERENE 1차 지표가 ISI), 변화량(사후−사전), 신뢰구간 포함·CI↔p 모순, `p = .07` 인데 "유의하게"라고 쓴 문장. 줄번호가 붙은 한국어 지적 목록 + `문제목록.csv`/`재계산표.csv`. **반올림·버림·올림을 모두 허용하는 구간 비교**로 오탐을 막고, 단측검정·Greenhouse–Geisser 등 단서가 있으면 치명 → 경고로 자동 강등하고 강등 사유를 적음. **리포트 첫머리에 '후보 N개 중 재계산 M개 / 건너뜀 K개(사유별)'를 반드시 출력** — 재계산 가능 claim 이 5개 미만이거나 **원고를 잘라 읽었으면** 조용히 통과시키지 않고 종료코드 3. 산술적으로 불가능한 보고(`r = 1.5`, `t(0)`, 음수 χ²)도 치명으로 지적. `draftcheck`(형식)·`citecheck`(DOI)와 함께 원고 3종 세트 (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 415개 테스트, 적대적 서브에이전트 11명이 3라운드로 찾은 결함 80건 수정 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `numcheck 원고.docx --out-dir 검토` (GRIM 은 `--scale ISI=0:28:7`) | 2026-08-13 (목) |
| [joinaudit-데이터병합감사](joinaudit-데이터병합감사) | 출처가 다른 여러 CSV/TSV/XLSX(워치 HRV·호흡·EEG 요약·수면일기·ISI·UT 로그) → **피험자 × 시점 한 장의 분석용 표 + 병합 감사**: 피험자 ID 표기 정규화(`S01`/`S1`/`BELL-001-01`/전각/공백 — **퍼지 매칭 없음**, `S01`과 `S02`는 절대 안 붙음), **자정 넘김 야간 귀속**(23:40과 다음날 03:20은 같은 밤), **중복 키에서 카테시안 조인 원천 차단**(pandas `merge`가 조용히 행을 곱하는 자리), **파일 간 키 겹침 검사**(표는 나왔는데 아무것도 안 붙은 상태 적발), N-흐름(입력→최종, 드롭 사유별 행 목록)·커버리지 매트릭스·**논문 Methods 초안(한/영)**. 산출물 `merged.csv`는 `longistat`에 그대로 투입(시점별 표라 `statwise`/`table1` 앞에는 피험자당 1행 요약이 필요하며, 리포트가 그 유사반복 경고를 실행마다 계산). **확신이 없으면 추측해서 붙이는 대신 종료코드 3으로 멈춤** (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 331개 테스트, 적대적 서브에이전트 4명이 찾은 결함 26건 수정 — `HARDENING.md`) | `실행.command` 더블클릭 · 또는 `joinaudit 워치.csv 일기.xlsx 설문.csv --align night --out-dir 결과` (먼저 `--inspect`) | 2026-08-07 (금) |
| [draftcheck-원고투고점검](draftcheck-원고투고점검) | 투고 직전 원고(.docx/.md/.tex/.txt) → **자기 정합성 전수 대조**: 본문 인용↔참고문헌(목록에 없는 번호·한 번도 인용 안 된 문헌·번호 순서), 그림/표 번호(미언급·유령 번호·건너뜀), 초록↔본문 표본수 불일치, 통계 보고(p=0.000·임계값만·효과크기/CI 누락), 약어 정의, 저널 분량 한도 — 줄번호 붙은 한국어 수정 목록 + `references.csv`로 citecheck 연결. **인식 못 하면 '이상 없음'이 아니라 '점검 불가'로 크게 경고** (외부 의존성 0, 네트워크 0, 원본 읽기 전용, 274개 테스트) | `실행.command` 더블클릭 · 또는 `draftcheck 원고.docx --limits 저널.json --out-dir 점검결과` | 2026-08-06 (목) |
| [medpath-매개효과분석](medpath-매개효과분석) | X(군)·M(매개)·Y(결과) 열이 있는 CSV → **매개효과(간접효과 a×b) 분석**: "중재가 *무엇을 거쳐서* 효과를 냈는가"를 부트스트랩 CI·공변량 보정과 함께 정량화 — BELL-001 호흡→부교감(RMSSD)→서파수면 기전 검증용. 인과 해석에 필요한 가정을 "할 수 없는 것"으로 문서에 명시 (외부 의존성 0) | `실행.command` 더블클릭 · 또는 `python3 -m medpath 데이터.csv --x arm --m rmssd_ms --y sws_min` | 2026-08-06 (목) |
| [rocdx-진단정확도분석](rocdx-진단정확도분석) | 검사값 열 + 기준진단 열 CSV → **ROC 곡선·AUC·최적 절단점·민감도/특이도·PPV/NPV·우도비**를 신뢰구간과 함께 계산하고 논문에 바로 붙일 문장까지 출력 (외부 의존성 0) | `실행.command` 더블클릭 · 또는 `python3 -m rocdx.cli 데이터.csv --score 검사값열 --truth 진단열` | 2026-08-06 (목) |
| [sleepdiary-수면일기지표](sleepdiary-수면일기지표) | 수면일기 CSV → **Consensus Sleep Diary 표준 정의**대로 TST·수면효율(SE)·입면잠복기(SOL)·WASO·수면중앙시각 계산, 대상자별→집단 2단계 집계 + 시기 간 대응표본 검정 — BELL-001 수면일기 지표용 (외부 의존성 0) | `실행.command` 더블클릭 · 또는 `python3 -m sleepdiary.cli 일기.csv --compare-periods baseline followup` | 2026-08-06 (목) |
| [metapool-메타분석](metapool-메타분석) | 연구 목록 CSV 한 장 → **메타분석 한 번에**: 효과크기 합성(고정·변량효과, τ² 4종)·이질성(Q·I²·예측구간)·하위군·leave-one-out 민감도·출판편향(Egger·Begg·trim-and-fill·깔때기그림)·텍스트 숲그림·KR/EN 문장 (외부 의존성 0) | `실행.command` 더블클릭 · 또는 `metapool 연구목록.csv --outcome "ISI 총점"` | 2026-08-02 (일) |
| [longistat-반복측정추이분석](longistat-반복측정추이분석) | 반복측정 long CSV → **반복측정/혼합 ANOVA·구형성 보정·MMRM(REML)·추세 대비·사후비교·기저 대비 변화량·ANCOVA·LOCF/BOCF 민감도·MCID 반응자 분석** — joinaudit `merged.csv`를 그대로 투입 가능, SERENE 시점별 ISI 추이용 (외부 의존성 0) | `실행.command` 더블클릭 · 또는 `longistat 데이터.csv --id 대상 --time 방문 --value ISI --group 군` | 2026-07-31 (금) |
| [powerplan-표본수설계](powerplan-표본수설계) | 설계 파라미터(또는 사전연구 CSV) → **표본수·검정력 계산**: 비중심 t/F 정확 계산, ANCOVA 기저보정·탈락·군집·다중비교·중간분석 반영, 생존·MMRM·비열등성·McNemar·kappa 설계 지원, **분석 n과 모집 n 구분** + KR/EN 프로토콜 문장 — IRB·연구계획서용 (외부 의존성 0) | `실행.command` 더블클릭 · 또는 `powerplan ttest2 --d 0.5 --power 0.8 --dropout 0.15` | 2026-07-30 (목) |
| [table1-기저특성표](table1-기저특성표) | 임상 CSV → **출판용 "표 1(기저 특성표)"** 자동 생성: 변수별 연속/범주 자동 판별 → 알맞은 요약(평균±SD / 중앙값[IQR] · n(%))·검정 자동 선택 + **군간 표준화평균차(SMD)**·결측 정리, Markdown/CSV/TSV/JSON 출력 — BELL-001 SERENE 등 임상시험 Table 1용 (외부 의존성 0, 121개 테스트 통과) | `실행.command` 더블클릭 · 또는 `table1 data.csv --group arm` | 2026-07-16 (목) |
| [agreestat-측정일치도](agreestat-측정일치도) | 두 측정방법 CSV → **일치도 분석**: Bland–Altman(bias·95% LoA+CI·비례편향 검정)+ICC(2,1)/(3,1)+Lin CCC+반복측정 CV — 비접촉 호흡/워치-HRV vs PSG/밴드 검증용, 논문 문장까지 (외부 의존성 0, ICC를 Shrout&Fleiss 정확값과 대조) | `실행.command` 더블클릭 · 또는 `agreestat data.csv -a 방법A -b 방법B [-s 대상id]` | 2026-07-13 (월) |
| [eegband-뇌파대역분석](eegband-뇌파대역분석) | 단일채널 EEG CSV → **대역파워**(delta/theta/alpha/beta/gamma 절대·상대)+**서파활동(SWA)**+SEF95·peak·slowing ratio, 에폭 단위 요약 — 직접 구현한 Welch PSD (BELL-001 EEG 서파수면 지표용, 외부 의존성 0, scipy welch와 ≤1e-14 일치) | `실행.command` 더블클릭 · 또는 `python3 -m eegband.cli eeg.csv --fs 128 --epoch 30` | 2026-07-13 (월) |
| [hrvkit-심박변이도분석](hrvkit-심박변이도분석) | RR/IBI(또는 HR) CSV → **HRV 지표 한 번에**: 시간영역(SDNN·RMSSD·pNN50)+주파수영역(LF/HF, 직접 구현한 FFT·Welch PSD)+비선형(Poincaré SD1/SD2·SampEn), 이소성박동 자동 보정 — BELL-001 호흡→부교감→RSA/HRV 기전 정량화 (외부 의존성 0, scipy와 ≤1e-6 일치) | `실행.command` 더블클릭 · 또는 `python3 -m hrvkit.cli rr.csv` (또는 `--json`) | 2026-07-13 (월) |
| [statwise-그룹비교통계](statwise-그룹비교통계) | 두 그룹/여러 그룹 CSV → **정규성(Shapiro-Wilk)·등분산(Levene) 자동 점검** 후 알맞은 검정(Student t·Welch·Mann-Whitney·ANOVA·Kruskal-Wallis)을 골라 실행 — 효과크기(Hedges g·rank-biserial·η²)+95% CI+Holm 보정 사후검정+**논문용 APA 문장**까지 (외부 의존성 0, scipy와 p값 ≤1e-9 일치, 오프라인 예제 포함) | `실행.command` 더블클릭 · 또는 `statwise 데이터.csv --value 값열 --group 그룹열` (또는 `--wide`) | 2026-07-10 (금) |
| [pubgap-논문공백탐색](pubgap-논문공백탐색) | 키워드 → PubMed 최근 동향 요약(연도별 발행량·주요 저널/주제·부상/쇠퇴)과 **덜 연구된 각도(연구공백)** 제안 — MeSH 공동출현 lift + 초기하 p값으로 "개별로는 흔한데 함께는 드문" 조합을 논문 주제 후보로 (외부 의존성 0, 오프라인 데모 포함) | `실행.command` 더블클릭 · 또는 `python3 -m pubgap.cli "slow breathing AND sleep" --email 내메일` | 2026-07-09 (목) |
| [factorscan-설문요인분석](factorscan-설문요인분석) | 설문 척도 CSV → 요인분석 적합성(KMO·Bartlett)·요인 수(고유값/Kaiser/평행분석)·요인적재량(Varimax)·공통성·수정된 문항-총점 상관을 한 번에 진단 (numpy만; SPSS식 척도 타당도 표를 재현가능하게) | `실행.command` 더블클릭 · 또는 `python3 -m factorscan.cli 설문.csv --config 설정.json` | 2026-07-02 (목) |
| [logflow-사용자로그분석](logflow-사용자로그분석) | 사용자 이벤트 로그 CSV → 세션화·이벤트/사용자별 집계·DAU/WAU/MAU·리텐션(코호트 day-N)·퍼널 전환율을 한 번에 요약 (표준 라이브러리만, tz 보정·결측 처리 지원) | `실행.command` 더블클릭 · 또는 `python3 -m logflow.cli 로그.csv --funnel 단계1,단계2` | 2026-06-29 (월) |
| [surveyscan-설문응답분석](surveyscan-설문응답분석) | 설문 응답 CSV → 문항별 기술통계·결측 요약·역문항 자동 재코딩·하위척도 점수·Cronbach α(신뢰도)·문항-총점 상관·문항제거시 α (표준 라이브러리만) | `실행.command` 더블클릭 · 또는 `surveyscan 설문.csv -c 설정.json` | 2026-06-26 (금) |
| [paperforge-논문아이디어발굴기](paperforge-논문아이디어발굴기) | 보유 데이터(EEG·워치·호흡·설문·MoA) 매니페스트에서 멀티모달 논문 아이디어 매트릭스 생성 — 가설·변수·분석법·저널·표본 실현가능성 | `실행.command` 더블클릭 · 또는 `paperforge manifest.json` | 2026-06-25 (목) |
| [citecheck-인용DOI검증](citecheck-인용DOI검증) | 원고 인용/DOI를 Crossref로 검증 — 깨진 DOI·메타데이터 불일치·철회 탐지 | `실행.command` 더블클릭 · 또는 `citecheck refs.bib` | 2026-06-25 (목) |

> `citecheck`는 단독 저장소([github.com/hyeonjoong/citecheck](https://github.com/hyeonjoong/citecheck))로도 공개돼 있습니다. 이후 만드는 툴은 이 모노레포 하위 폴더로 쌓입니다.

## 새 툴 바로 쓰는 법

각 툴 폴더에서 한 번만 설치하면 명령어가 전역으로 등록됩니다:

```bash
cd ~/Downloads/02_프로젝트/깃헙/<폴더이름>
python3 -m pip install -e .
```

또는 그냥 폴더 안의 **`실행.command`를 더블클릭**하세요.

## 최신 상태로 당기기

자동 실행이 새 툴을 푸시하므로, 가끔 최신으로 맞추려면:

```bash
cd ~/Downloads/02_프로젝트/깃헙
git pull
```
