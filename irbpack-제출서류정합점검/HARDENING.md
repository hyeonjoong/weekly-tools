# HARDENING — irbpack 적대적 검토 기록

이 툴은 사람 검토 없이 배포되므로, 기획서가 요구한 대로 **실제 서브에이전트**로 적대적 검토를
돌리고 그 결과를 여기에 남깁니다. 라운드 0 은 코드를 한 줄도 쓰기 전에 한 실측입니다.

빌드 메모: 2026-08-30 자동 실행이 `__init__.py` · `__main__.py` · `mask.py` 3개 파일(81줄)만 남기고
끊겼습니다. 2026-09-08 사용자 지시로 기획서를 그대로 이어받아 완성했습니다.

---

## 라운드 0 (2026-08-28, 기획 단계) — 코드 이전의 30분: "정말 어긋나 있는가?"

기획서 "실패할 수 있는 지점" 1·2·3번은 전부 같은 질문입니다 — *이 사람이 이미 손으로 다 맞춰 놓아서
매번 "치명 0건"만 뱉는 툴이 되지 않는가*. 그래서 기획 단계에서 **이미 IRB 승인이 난** 실제 패킷 6종에
12개 항목 정규식을 돌렸습니다.

| 항목 | 연구계획서 | ICF 성인용 | ICF 보호자용 | Assent | CRF | 모집광고 |
|---|---|---|---|---|---|---|
| 방문 횟수 | `2회 방문` | `2회 방문` | **`1회 또는 다회기 방문`** | — | **Visit 1~6** | `방문 1` |
| 소요시간 | — | `90분` / `120분` | **`60분` / `180분`** | `30분` / `1시간` | — | — |
| 보상 | `교통비 실비` | **`시간당 10,000원(사례비 포함)`** | 동일 | — | — | — |
| 연락처 | — | 사무실 | 동일 | — | — | **개인 휴대전화** |
| 표본수 | 90/30/20 | 동일 | 동일 | — | — | 30/20 (총 90 없음) |
| 연령·보관기간·제목·버전 | 전부 일치 | | | | | |

**결론: 승인 패킷에서도 치명 3 · 경고 2 가 남습니다.** 이 숫자가 `tests/test_kobio.py` 의 정답이고,
규칙을 넓히다 이 숫자가 **늘어나면** 테스트가 깨집니다 (`test_no_more_findings_than_truth`).
픽스처는 추출된 문장만 옮기고 이름·기관·전화·제목을 전부 합성으로 바꿨습니다.

### 기획서에서 의도적으로 좁힌 규칙 (근거 있음)

1. **전파 누락은 "일부만 있을 때"만 경고합니다.** 기획서는 "프로토콜에 있는데 ICF/CRF 에 값이 없다 → 경고"
   라고 썼지만, 그대로 구현하면 Assent 에 표본수가 없고 CRF 에 소요시간이 없는 것까지 전부 경고가 되어
   KoBio 정답(경고 2)을 통과하지 못합니다. 값이 **아예 없는** 문서는 울지 않고, **일부만** 있는 문서만
   경고합니다 — 안 적는 게 맞는 문서도 있습니다.
2. **값 충돌 규칙은 "합집합을 다 말하는 문서가 하나도 없을 때"입니다.** 처음엔 "서로 부분집합이 아닌
   쌍이 하나라도 있으면 충돌"로 구현했는데, 라운드 1 이 프로토콜 {90,45} · 동의서 {90} · 공고 {45} 라는
   정상 패킷을 치명으로 만드는 것을 잡았습니다.
3. **이메일은 문서 간에 달라도 경고하지 않습니다.** 프로토콜은 책임자 메일, 동의서는 IRB 사무국 메일을
   적는 것이 정상입니다. 전화번호만 봅니다.
4. **`← 다름` 표시는 역할 순서상 첫 문서(프로토콜)를 기준으로 합니다.** 이것은 "프로토콜이 맞다"는
   판정이 아니라 표시 기준일 뿐이며, 리포트 문구 어디에도 어느 값이 맞다는 말은 없습니다(금지어 테스트).

---

## 라운드 1 (2026-09-08) — 서브에이전트 3명 병렬 (스펙 대조 · 추출/대조 · IO/안전)

433 → 437 테스트. 아래는 확인된 결함과 수정입니다. 재현하지 못한 주장은 적지 않았습니다.

### 판정이 틀리던 것 (치명/경고가 잘못 뜨거나 안 뜨던 것)

| # | 결함 | 수정 |
|---|---|---|
| 1 | `연구책임자에게 문의하십시오` 에서 책임자 이름이 **"에게"** 로 추출되어 정상 패킷에 치명/경고 | 라벨 뒤 두 글자 이상 조사·접속어(에게·로부터·또는·께서·이며…)를 이름에서 제외. 한 글자 조사는 이름 최소 길이에 걸려 애초에 안 잡힘 |
| 2 | 동의서의 **개정 이력** 행(`버전 1.0 (…) 최초 작성; 버전 1.2 (…) 개정`)이 첫 매치라 문서 버전·날짜가 1.0 으로 잡혀 치명 2건 | 개정 이력·history·최초 작성 문장과 **버전이 둘 이상 적힌 문장**은 버전 후보에서 제외 |
| 3 | **부분집합 쌍 규칙**: 프로토콜 {90,45} · 동의서 {90} · 공고 {45} 가 치명 | "모든 값을 다 말하는 문서가 하나도 없을 때"만 충돌. 위 경우는 전파 누락 경고 |
| 4 | `총 연구 기간은 2년` 이 **참여 기간**으로 잡혀 동의서의 `참여 기간 1년` 과 치명 | `연구 기간` 은 참여 기간 패턴에서 제거 |
| 5 | `임상시험보험 … 보상 한도 1인당 100,000,000원`, `연구비 예산 5,000,000원` 이 **보상**으로 추출 | 보험·손상·배상·예산·연구비·한도·치료비 문장은 보상에서 제외 |
| 6 | `대상자 1명당`, `보호자 1명`, `선행 연구에서는 120명`, `1명의 연구자가 30명의 참여자를` 이 표본수로 | 1명은 표본수로 세지 않음(비율·단위 표현), 배경·선행연구 문장 제외, 숫자 **뒤** 명사가 대상자면 앞의 연구자 무시 |
| 7 | `주 1회 방문하여 총 8회`, `제2회 방문 시`, `3회차 방문` 이 방문 횟수로 | 주/월/매주 뒤의 횟수는 빈도, 제N회·N회차는 서수로 제외 |
| 8 | `제2회 방문 시 혈액검사` 가 평가항목 **"제2회 방문 시 혈액검사"** 로 | 방문·회기 서수 접두를 떼고 `혈액검사` 만 |
| 9 | `금전적 보상은 없으며 교통비만 지급` vs 동의서 `회당 3만원` 이 **exit 0** (없음 표현 미인식) | 없으며·없고·없음·따로 없 형태 추가 → 치명 |
| 10 | `만 19세 이상 45세 미만` 과 `만 19~45세` 가 같다고 판정 | `미만` 은 상한 −1. 이제 다름 |
| 11 | `1년 6개월` 이 {12개월, 6개월} 로 쪼개져 `18개월` 과 치명 | 년+개월 합산 |
| 12 | `작성일: 2026. 8. 25.` 이 문장 분리에 잘려 날짜 추출 실패 → 날짜 축이 조용히 빠짐 | 숫자 뒤 마침표에서는 문장을 자르지 않음 |
| 13 | `…척도(ISI), …지수(PSQI), …척도(HADS)를 평가한다` 가장 흔한 평가항목 문장이 추출 0 | 문맥 패턴에 `를 평가/측정/시행/실시/작성한다` 추가 |
| 14 | `Version 1.0.`(문장 끝 마침표)·`ICF_V2.md` 에서 버전 미추출, `v2` vs `v2.0` 이 치명, `Version 2026.05.18` 이 버전 | 버전 정규형(`2.0`≡`2`≡`2.0.0`), 1900 이상은 날짜형으로 거부 |
| 15 | `5천원`·`3만5천원`·`1억원` 금액 미인식 | 천/만/억 단위 파서 |
| 16 | 프로토콜 이메일과 동의서 IRB 사무국 이메일이 달라 **경고** | 이메일 불일치는 경고하지 않음 (라운드 0 의도적 규칙 3) |
| 17 | **정규화 쌍 카운터**가 연령·날짜·버전에서 항상 0 (raw 가 이미 정규화된 표시형이라 원문 차이를 못 봄) | `Extraction.source` 에 원문 토큰 보관, 카운터는 그것으로 비교. 라벨(`Version`/`버전`) 차이는 세지 않음 |
| 18 | **`--baseline` 이중 보고**: 같은 옛 값이 "값 충돌" + "개정 미반영" 두 치명으로 | '다름' 문서가 전부 개정 미반영으로 잡힌 충돌은 접음 (`fold_baseline`) |
| 19 | 같은 파일명(`v1/동의서.md`, `v2/동의서.md`)이 **한 문서로 뭉쳐** 2회 vs 3회 충돌이 exit 0 으로 사라짐 | 파일명이 겹치면 상위 폴더로 구분 |
| 20 | `w:sdt`/`w:customXml` 로 감싼 표 행·셀·문단이 **조용히 누락** (Word 반복 구역·양식 컨트롤) | 래퍼를 투과하는 행·셀 탐색 |
| 21 | 변경 추적으로 **삭제된 표 행**(`w:trPr/w:del`)·`w:moveFrom` 이 현재 내용으로 읽힘 — "모두 수락" 이라던 설명과 불일치 | 삭제 행·이동 원본·`mc:Fallback`(텍스트상자 중복 사본) 제거 |
| 22 | CRF 가 없는 패킷에서 평가항목 판정 사유가 불명 | 커버리지에 "CRF 문서 없음" 으로 자백 |

### 크래시·안전·유출

| # | 결함 | 수정 |
|---|---|---|
| 23 | `mask._EMAIL` 이 긴 ASCII 문자열에서 **O(n²)** — 100k 자에 76초, PDF 텍스트에서 현실적 | 앞쪽 lookbehind 로 토큰 중간 시작 차단. 200k 자 < 2초 테스트 |
| 24 | 4300자리 숫자에 `int()` 가 `ValueError` → **exit 1(치명 발견)로 둔갑** | 정규식 자릿수 상한 + 내부 예외는 전부 exit 2 ("내부 오류로 판정하지 않음") |
| 25 | **암호 걸린 docx**(`RuntimeError`)·미지원 압축(`NotImplementedError`)·깨진 스트림(`zlib.error`) 이 트레이스백 + exit 1 | 읽지 못한 문서로 자백 → exit 3 |
| 26 | PDF FlateDecode **압축 폭탄** — 814KB 파일이 6.2GB 메모리, 3천만 문단 | 압축 해제 50MB 상한 + 문단 수 상한 |
| 27 | **CID 폰트(Identity-H) PDF** 가 글리프 번호 쓰레기를 "읽음"으로 통과 — 한국어 PDF 대부분 | `/Identity-H`·`/CIDFontType` 감지 시 거부 + 한글/영문 단어 10개 미만이면 거부 |
| 28 | **파일명의 전화·이메일**이 `정합점검.md` 와 exit 2 경로 콘솔에 그대로 | 모든 출력 경로가 마스킹 + 새니타이즈를 거침 (`cli._clean`, `report._md`) |
| 29 | `+82-10-…`, `(02)123-4567`, `010 - 1234 - 5678`, `1588-xxxx`, 주민번호 7번째 자리 5~8, 붙여 쓴 13자리, 전각 숫자, 한글 로컬파트 이메일 미마스킹 | 전부 마스킹 |
| 30 | argv(`경로가 없습니다: …`, `--out-dir` 경로)의 개행이 콘솔에 가짜 `[치명]` 줄 | 새니타이즈 |
| 31 | 폴더 안 **심볼릭 링크가 실물을 가려** 실물 CRF 가 한 번도 읽히지 않음 | 폴더의 링크는 수집하지 않고, 명시적 링크 인자는 읽지 못한 문서로 |
| 32 | `.tex` 가 폴더에서 **조용히 무시** — 강제 장치 1(원고 → exit 2)이 명시 인자에만 동작 | `.tex` 수집 → 원고 감지 exit 2 |
| 33 | `--out-dir link/`(끝 슬래시)로 심볼릭 링크 검사 우회 | 끝 슬래시 제거 후 검사 |
| 34 | 리포트 쓰기 실패 시 이미 `→ exit 0` 을 찍은 뒤 exit 2 | 쓰기를 먼저 하고 콘솔을 찍음 |
| 35 | `check_coverage` 가 `render_console` 에만 있어 `render_md`/`write_all` 이 자백 없이 나갈 수 있음 | 세 곳 모두 강제. md 본문에 블록이 없어도 거부 |
| 36 | 커버리지 헤드라인이 하위 항목(기관·참여기간)을 섞어 "대조 11 + 대조 불가 2 = 13" | 하위 항목은 "하위 항목 빠짐" 줄로 분리, 12항목 합계·중복·비항목 이름을 코드로 거부 |
| 37 | 근거 **문장**이 콘솔·md 에 인쇄되지 않음 (기획서 "근거 문장과 함께") | 근거 줄 아래 「문장」 인쇄 (120자 절단) |
| 38 | `[문서 역할]` 이 파일명 순이라 프로토콜이 맨 아래, `[정보]` 에 값이 없음 | 프로토콜 → 동의서 → 승낙서 → CRF → 공고 순, 일치 항목에 값 표시 |
| 39 | 마스킹 규칙 예시가 **실제 개인 휴대전화**(기획서에 인용된 번호)를 그대로 부록에 인쇄 | 예시 번호를 `010-0000-1234` 로 |

라운드 1 에서 **확인했지만 결함이 아니었던 것**: billion-laughs 엔티티 확장(expat 이 거부), 압축 헤더 위조로
`MAX_BYTES` 우회(불가), ASCII 콘솔에서 4개 종료 경로 전부 생존, 입력 덮어쓰기 경로(전부 거부, 하드링크
피해 파일 무사), 3 > 1 우선순위.

---

## 라운드 2 (2026-09-08) — 서브에이전트 2명 병렬 (추출/대조 · IO/리포트/CLI)

라운드 1 수정이 전부 유지되는지 재확인하고(두 검토자 모두 "유지됨"), 라운드 1 의 합성 패킷과 **다른 기관 양식**
(병원식 표지 표 · `4.3` 두 단계 번호 · ①② 기준 · CRF 선정기준 체크리스트 · 존댓말 동의서 · 대학 온라인 설문 연구)
으로 정상 패킷 2종을 새로 써서 돌렸습니다. 437 → 468 테스트.

### 판정이 틀리던 것

| # | 결함 | 수정 |
|---|---|---|
| 40 | CRF 선정기준 **체크리스트의 헤더 행**(`기준 │ 예 │ 아니오`)이 기준 항목으로 세어져 정상 패킷에 "개수 다름" 경고 2건 | 체크리스트 어휘·체크박스만 있는 행은 항목이 아님 |
| 41 | 프로토콜·동의서 쪽 평가항목 추출이 0 이면 CRF 단독 폼이 **"한 문서에만 존재"** 로 넘어가 동의 범위 밖 폼이 조용히 사라짐 | 동의 범위 밖 규칙은 CRF 폼 대 프로토콜·동의서 **본문**이라, 추출 0 이어도 성립하도록 순서 변경 |
| 42 | `4.3 평가 항목`·`4.3.1 주요 평가 변수` 절 제목이 평가항목 **"3 평가"** 로 추출 → 정상 패킷에 전파 누락 경고 (한 단계 번호 `4. 평가 항목` 만 테스트해 놓쳤음) | 절 제목 문단 제외, 토큰은 글자로 시작해야 함 |
| 43 | `최대 220명까지`, `대부분 100명 미만`, `…을 대상으로 한 연구는` 이 표본수로 | 상한·하한 표현(최대·최소·미만·초과·까지·이내)과 배경 문장 제외 |
| 44 | **합집합 규칙의 약점**: 동의서가 `총 4회 방문` 과 `Visit 1~3` 을 같이 말하면 그 문서가 "합집합을 다 말한" 셈이 되어 치명이 아니라 남들의 전파 누락으로; 한 문서에 책임자 두 명도 마찬가지 | 문서당 값이 하나여야 하는 하위키(제목·버전·날짜·책임자·기관·방문횟수·참여기간·보관기간)는 **완전 일치**만 봄. 집합형(표본수·연령·소요시간·보상)만 합집합 규칙 |
| 45 | `…척도(PSS)를 작성하시게 됩니다`·`받으시게 됩니다`(존댓말) 와 `다음 설문을 실시한다.` 뒤 **다음 문장의 목록**이 추출 0 | 존댓말 동사 추가, 리드인 문장 뒤 3문장을 문맥으로 이어 봄 |
| 46 | 병원식 표지 셀 `연구책임자 소속/직위/성명 │ 가상대학교병원 재활의학과 / 교수 / 김가상` 과 `연구책임자 │ 김가상 (가상대학교병원 …)` 에서 이름·기관 미추출 | 라벨/값을 `/` 로 짝지어 추출, 괄호 안 기관명 추출 |
| 47 | `통증숫자평가척도(NRS)` 가 게으른 매치로 **"통증숫자평가"** 에서 잘림 | 접미(평가·척도·검사…) 연쇄를 끝까지 소비 |

### 크래시·안전·유출

| # | 결함 | 수정 |
|---|---|---|
| 48 | 비UTF-8 파일명(cp949 zip 을 리눅스에서 푼 것) + `--out-dir` 에서 `UnicodeEncodeError` → 트레이스백 **exit 1** + 0바이트 `정합점검.md` | 서로게이트를 파일을 열기 전에 걸러 냄, `ValueError` 도 출력 실패로 잡아 exit 2 |
| 49 | 닫힌 파이프(`\| true`)·없는 stdout(`>&-`) → `BrokenPipeError`/`AttributeError` 로 **exit 1** — OS 수준 실패가 "치명 발견"으로 둔갑하는 마지막 경로 | `_print` 가 어떤 출력 실패도 삼킴 (종료코드는 판정만 정함) |
| 50 | 읽을 수 없는 입력 **폴더**(`chmod 000`) → `PermissionError` 트레이스백 exit 1 | exit 2 + 메시지 |
| 51 | 상위 폴더 이름까지 같은(`p/a/ICF.md`, `q/a/ICF.md`) 두 문서가 여전히 한 문서로 뭉쳐 90명 vs 80명이 **exit 0** | 그래도 겹치면 번호(`#1`, `#2`)로 구분 |
| 52 | 근거 문장을 **마스킹 전에** 240자에서 자르면 전화·주민번호가 반쯤 잘린 채(`010-9876-`) `항목추출표.csv` 에 노출 | 공백 경계까지 늘려 자름 — 번호 한가운데를 자르지 않음 |
| 53 | 유니코드 대시(`010‐5555‐6666` U+2010 등)·`+82 (0)10-…`·소프트하이픈·워드조이너가 낀 번호가 마스킹도 추출도 안 됨 | 숫자 사이의 대시류를 `-` 로, 보이지 않는 문자 제거 (문장 속 일반 대시는 건드리지 않음) |
| 54 | PDF 상한이 **스트림당**이라 30MB짜리 25개(2.5MB 파일)가 5분 넘게 돌다 죽음, 문단 수 검사도 다 읽은 뒤 | 누적 50MB 상한, 문단 수는 읽는 중에 검사 |
| 55 | `w:noBreakHyphen`(`만 5‑12세` → `만 512세`)·`w:softHyphen`·`w:sym` 이 조용히 사라져 연령이 대조불가로 | `-`/``/`·` 로 방출 |
| 56 | `--role 010-…=x` 의 역할 이름이 오류 메시지에 그대로 | 마스킹 |
| 57 | 못 읽은 사유에 **절대경로**(홈 디렉터리 사용자명)가 들어가 리포트에 새고, 마스커가 경로의 16진수를 전화번호로 뭉갬 | 사유에서 경로를 파일명으로 치환, `출력:` 은 사용자가 준 경로 그대로 |
| 58 | `--out-dir ''` 이 조용히 무시되어 파일 없이 exit 0 | exit 2 |
| 59 | `대조불가.csv` 에 하위 항목이 섞여 콘솔의 "대조 불가 N개" 와 행 수가 다름; 출력 이름 자리에 **폴더**가 있으면 "하드링크입니다" 라는 엉뚱한 메시지 | `구분` 열(항목/하위 항목) 추가, 폴더는 폴더라고 말함 |

라운드 2 에서 **확인했지만 결함이 아니었던 것**: 정규식 안전성(모든 추출기가 300KB 입력에서 1초 미만, 전체 CLI
2.9초), KoBio 정답이 규칙 변경 뒤에도 **우연이 아니라 그 이유로** 정확함(방문 합집합을 아무도 안 말함 · 동의서
소요시간 3쌍 서로소 · 실비 vs 시간당 · 공고 ⊂ 프로토콜 · 010 이 참조에 없음), 일부러 어긋뜨린 12종(연령 +1 ·
보상 · 승낙서 구버전 · CRF 추가 폼 · 소요시간 · 표본수 · 방문 · 제목 · 보관기간 · 책임자 · 공고 휴대전화)이
전부 **정확히 그 항목·그 심각도**로만 잡힘, `KeyboardInterrupt` 가 exit 2 로 삼켜지지 않음, FIFO·`x.docx` 라는
이름의 폴더·깨진 링크는 건너뜀, `PYTHONIOENCODING=cp949|ascii`, `LANG=C` 에서 종료코드 0/1/2/3 전부 정상.

### 남아 있는 알려진 한계 (수정하지 않음, 근거 있음)

- **집합형 항목의 합집합 규칙**은 "프로토콜 {90,45,30} · 동의서 {45} · 공고 {30}" 을 경고로 봅니다. 동의서와
  공고가 서로 다른 군만 적은 것이 실제 불일치일 수도 있지만, 경고문에 "동의서에 90/30 없음; 공고에 90/45 없음" 이
  전부 인쇄되므로 사람이 볼 수 있습니다. 치명으로 올리면 라운드 1 의 정상 패킷이 다시 치명이 됩니다.
- **중첩 표**는 바깥 셀에 평탄화됩니다. 중첩 표 안의 CRF 폼 목록은 추출되지 않을 수 있습니다 (커버리지에 "CRF 에서
  폼 이름이 추출되지 않음" 으로 자백).
- 마스커가 긴 16진수 문자열(`b3a07386-7761`)을 전화번호로 보고 가릴 수 있습니다 — 과잉 마스킹은 유출보다 쌉니다.
- **실제 SUNRISE-K · BELL-002 패킷**은 이 세션(클라우드)에서 접근할 수 없어 돌리지 못했습니다. 기획서의
  "두 패킷 연속 치명 0건이면 접는다" 는 사용자 로컬 실행으로 확인해야 합니다.
