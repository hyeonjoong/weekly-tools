"""joinaudit — 출처가 다른 여러 CSV/XLSX를 피험자×시점으로 병합하고 그 과정을 감사한다.

이 패키지가 지키는 두 가지 불변식(나머지를 다 빼도 이 둘은 남는다):

1. **중복 키에서 카테시안 조인이 절대 일어나지 않는다.** 병합 전에 파일별로
   키 중복을 해소하거나(명시적 정책) 그 키를 통째로 제외하므로, 출력 행 수는
   어떤 입력에 대해서도 키 집합의 크기를 넘지 않는다.
2. **어떤 입력 행도 사유 없이 사라지지 않는다.** 모든 입력 행은 정확히 하나의
   처분(사용 / 드롭 사유)을 배정받고, `입력 = Σ드롭 + 사용`이 항상 성립한다.

전부 표준 라이브러리만 사용하며 네트워크 호출이 없다.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
