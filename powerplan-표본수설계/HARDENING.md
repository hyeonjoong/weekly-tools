# HARDENING — 적대적 검토 이력

이 툴은 사람 리뷰 없이 배포되므로, 만든 뒤 **독립적인 적대적 검토 패널**을 여러 라운드
돌려 결함을 찾고 고쳤다. 각 라운드는 서로 다른 역할의 검토자를 **동시에** 띄워 서로의
결론을 모르는 상태로 감사하게 했고, 나온 결함은 전부 고친 뒤 다음 라운드를 시작했다.

검토 축: ① 정확성(수식 재유도) ② 엣지케이스/견고성 ③ 연구자 유용성 ④ 문서 정직성
⑤ 테스트 품질(돌연변이 테스트) ⑥ 보안·임상데이터 취급 ⑦ 성능·수치 한계

---

## 라운드 1 — 7명 패널 (동시 실행)

검토자들은 scipy·mpmath·몬테카를로를 **독립 오라클**로 써서 모든 수식을 다시 계산했고,
패키지를 /tmp에 복사해 돌연변이 95종을 심어 테스트가 잡아내는지 확인했다.

### 확인된 것 (수정 불필요)

- 8개 설계의 검정력 수식 전부 재유도 통과. scipy `nct`/`ncf`와 최대 절대오차 ~1e-12,
  TOST 정확 적분은 mpmath 30자리와 1e-14 일치, 몬테카를로(40만 회)와 MC 오차 내 일치.
- `smallest_unit`이 실제로 **최소** n을 준다는 것을 8설계 × 600조합 전수 대조로 확인.
- 네트워크 접근 0, 쓰기 경로는 `-o` 하나, `lru_cache`에 환자 데이터가 남지 않음.
- ANOVA의 λ = f²·N (총 N) 관례, Cohen's f의 ÷k 정의, Bonett/Bland–Altman 공식 모두 정확.

### 고친 결함 (심각도 순)

| # | 결함 | 조치 |
|---|---|---|
| 1 | **`icc`/`loa`의 프로토콜 문장이 신뢰수준을 "95%"로 하드코딩** — `--alpha 0.1`이면 표본수는 90% 기준인데 붙여넣을 문장은 95%라고 주장 (IRB 문서에 들어가는 문장) | `1 − alpha`에서 유도. 회귀 테스트 `test_precision_sentences_follow_alpha` |
| 2 | **긴 군 라벨이 조용히 합쳐져 표본수가 틀림** — 표시용으로 60자로 자른 문자열을 집계 키로 써서, 앞 59자가 같은 두 군이 하나로 병합 (d가 −19 → −2.25로, n이 5/군으로) | 집계는 원본 라벨, 표시만 절단. 표시 라벨이 겹치면 `#2` 부여 |
| 3 | **비율 비교 연속성 보정의 아래쪽 꼬리 부호 오류** + 보정이 효과를 다 먹으면 검정력을 0.0으로 보고 (실제 ~3%) | 기각역 `|p̂1−p̂2| − cc > z·SE`를 양쪽 꼬리에 일관 적용, 클램프 제거. 독립 재계산과 1e-12 일치 |
| 4 | **트레이스백 6종** — `--alpha 2 --alpha-method sidak`(복소수), `--conf 1`, `--n 1e16`(자유도 오버플로), `--f 1e20`(비중심 F 급수), 극단값 JSON(NaN), 깨진 따옴표 CSV | 설계 생성 **전에** 공통 입력 검증(`_validate_common`), `MAX_DF`/`MAX_NCF_TERMS` 상한, `csv.Error`→한국어 메시지, JSON은 비유한값을 null로 |
| 5 | **군집설계의 "분석 표본수"가 설계효과를 빼먹음** — 유효 n 64를 분석 대상으로 표시(실제 필요 93), 게다가 "모집 162명"과 "군집 17개×10명=170명"이 동시에 표시돼 모순 | 3단 구조로 재설계: 유효 n → ×DE = **분석 대상** → ÷(1−탈락) → 군집 단위로 올린 **모집** |
| 6 | **사전연구 계획이 "권장(보수적)"이라 써놓고 헤드라인·프로토콜 문장은 관측값(낙관적)을 사용** — 와우핏 예제에서 35쌍을 권하면서 문장은 10명 | 기본값을 보수적 하한으로 전환(`--plan-on`), 신뢰구간이 0을 포함하면 관측값으로 되돌리고 크게 경고. 마크다운 출력에도 경고 추가 |
| 7 | **와우핏 예제가 중재군·대조군을 섞어 전후 비교** (dz 1.02 = 1.57과 0.64의 혼합), 게다가 한쪽만 고를 방법이 없었음 | `--filter 열=값` 추가. 문서·`실행.command` 예제 모두 `--filter 군=중재` 사용 |
| 8 | **CSV가 스트리밍이 아니었음** — 파일 전체를 bytes→str→StringIO로 3중 적재(217MB 파일에 1.3GB RSS), README는 "스트리밍"이라 주장 | 앞 64KB만 읽어 인코딩·구분자 판별 후 파일 객체를 그대로 `csv.reader`에 전달. 100만 행 4초·25MB로 확인(테스트 `test_csv_reading_is_streaming`) |
| 9 | **`-o` 저장이 조용히 덮어쓰고 심볼릭 링크를 따라감**, 권한 0644 | `O_EXCL|O_NOFOLLOW`, 권한 0600, 덮어쓰려면 `--force` |
| 10 | 문서 허위·과장 5종 — 존재하지 않는 `HARDENING.md` 링크, 실제로 없는 scipy 교차검증 주장, "공통 옵션"이 `icc`/`loa`에는 없음, "정확히 α", 전역 "비중심 t/F 정확계산"(비율·상관은 근사) | 전부 수정. 이 파일도 그래서 존재한다 |
| 11 | 영문 문장 비문 — "**A two one-sided t-tests**", "with non-inferiority margin with one-sided α", "to **detect** a margin"(마진은 검출 대상이 아니다), LoA의 "within-subject SD"(정의가 다름) | 관사 처리 함수, 설계별 동사(exclude/demonstrate), 용어 정정 |
| 12 | 한국어 조사 오류 — "80.0%**을** 위해서는"(항상 틀림), "0.208**를**" | 숫자·한글·%를 읽는 종성 판정 함수(`_josa`) |
| 13 | `hedges_correction(df=1)`에서 조용히 J=1.0으로 대체해 **보정 안 된 값을 보정값처럼** 표시 | 쌍 2개(df=1)에서는 `hedges_g`를 `None`으로 |
| 14 | `loa_plan` 오프바이원(n=2가 충분한 경우), `icc_ci_width`의 `raters=1`에서 `ZeroDivisionError` | 경계 검사 추가 |
| 15 | 비열등성 효과크기 라벨이 `(마진±차이)/SD`인데 "마진(표준화)"이라 표시 → 민감도 표의 "효과×0.8" 열도 실제로는 ×0.7 | `margin/sd`로 통일(동등성과 일치), 유효 여유는 별도 필드 |
| 16 | `t_cdf`가 \|t\| > 9.5e7에서 **조용히 0/1로 포화**(1−x가 1.0으로 반올림) → `t_ppf`가 3e8 대신 9.5e7 반환 | `betainc`에서 x로 되살리는 경로 추가 |
| 17 | CSV 값의 터미널 이스케이프·양방향 오버라이드·폭 0 문자·스프레드시트 수식(`=cmd|...`)이 출력까지 전달, 마크다운 표의 `|`가 표를 깨뜨림 | `_CTRL` 확장(C1·LS/PS·bidi·ZW), 수식 선행문자 앞에 `'`, 마크다운 셀 이스케이프 |
| 18 | 오류 메시지·저장 파일에 **원본 셀 값**이 들어감(자유기술 값이면 재식별 위험), 절대경로 노출 | 저장물에는 행·열 위치만, 경로는 파일명만 |
| 19 | 예제 CSV가 실제 환자 데이터와 구분되지 않음 (내부 프로토콜 코드까지 표기) | `examples/README.md`에 합성 데이터 명시, 코드 표기 제거 |
| 20 | 돌연변이 11종 생존 (테스트 구멍) — prop2의 가중 합동비율, 4개 설계의 아래쪽 꼬리 누락, TOST 클램프 제거, noninf의 df, 대응표본 Hedges g 방향, `conservative_d`의 min/max, `log_beta` 대칭 | `tests/test_hardening_r1.py`(72개)로 전부 사살. 검정의 크기 테스트를 8설계 × 양측/단측으로 확장 |

### 유용성 검토가 요구한 기능 (구현)

- **ANCOVA / 변화량 설계배율** (`--analysis ancova|change`, `--baseline-r`): 기저값이 있는
  연구에서 추적값만 비교하면 표본수가 **2배**로 부풀려진다는 지적. Frison & Pocock(1992) /
  Borm et al.(2007)의 (1−r²), 2(1−r) 배율을 정확 비중심 t 위에 구현. 예제 사례에서
  군당 166명 → 83명. 검토자가 계산한 SERENE 시나리오(r=0.711)와 일치.
- **사전연구에서 r과 탈락률 추정** (`--baseline` 열): "파일럿이 실제로 추정할 수 있는 두 값"
  이라는 지적을 반영. 관측 결측률과 군내 상관을 다음 명령에 그대로 넣을 수 있게 출력.
- `--conf`의 문헌 권고(60~80% 하한; Browne 1995, Kieser & Wassmer 1996)를 notes에 명시.

### 남긴 것 (의도적)

- `--sidak`, `--continuity`, `--bias-correct`를 삭제하라는 제안은 받아들이지 않았다.
  세 옵션 모두 심사자가 다른 도구로 재현할 때 값을 맞추는 데 쓰이고, 기본값이 아니라
  명시적 선택이며, notes에서 한계를 설명한다. 대신 `--continuity`의 **출처 표기 오류**
  (Casagrande–Pike–Smith → Yates 보정)를 바로잡았다.
- 검정력 기본값 0.80은 유지했다(관례). 3상 확증시험에서 0.90을 권하는 문구를 README에 추가.
- 성능 최적화는 하지 않았다. 실측 결과 전형적 실행 134ms(대부분 인터프리터 시작),
  최악의 `--sensitivity` 0.5초로 이미 충분하며, 검토자도 "이 툴의 실사용에는 최적화 불필요"로 결론.

---

*(라운드 2 진행 중 — 결과는 이어서 기록)*
