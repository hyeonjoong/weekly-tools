"""revcheck — 리비전 응답 점검.

제출본(old) · 개정본(new) · 응답서(response) 세 파일을 동시에 읽어,
**응답서에 적은 약속이 개정본에 실제로 반영됐는지**를 기계적으로 대조한다.

이 패키지가 보는 것은 딱 두 가지다.
  1. 리뷰어 코멘트 번호가 하나도 빠지지 않고 응답됐는가 (번호 전수 점검)
  2. 응답서가 "이렇게 고쳤습니다"라며 인용한 문구가 개정본에 **문자 그대로** 있는가

나머지(미신고 변경·위치 참조·참고문헌 증감)는 곁가지다.
응답의 내용적 타당성은 판단하지 않는다 — 그것은 사람의 일이다.

외부 의존성 0, 네트워크 호출 0, 원본 파일은 절대 수정하지 않는다.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
